# Radar v2 Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build Radar v2 — a fully automated cold email acquisition engine with 11-stage lead intelligence, GWS dual-inbox sending, 4-layer anti-spam, and a 9-page React dashboard.

**Architecture:** Two PM2 processes — `radar-cron` (cron.js fires all 9 scheduled jobs) and `radar-dashboard` (dashboard/server.js Express API on port 3001). Nginx proxies radar.simpleinc.cloud to port 3001, serves React SPA for `/`. All state in a single SQLite file (WAL mode).

**Tech Stack:** Node.js 20 ESM, better-sqlite3, @anthropic-ai/sdk, @google/generative-ai, nodemailer, imapflow, node-cron, vitest, React 18 + Vite, Express, JWT.

---

## Chunk 1: Foundation

**Files:**
- Create: `package.json`
- Create: `db/schema.sql`
- Create: `utils/db.js`
- Create: `utils/sleep.js`
- Create: `utils/telegram.js`
- Create: `ecosystem.config.js`
- Create: `cron.js`
- Create: `tests/utils/db.test.js`
- Create: `tests/utils/sleep.test.js`
- Create: `tests/utils/telegram.test.js`

---

### Task 1.1: package.json + install deps

- [ ] **Step 1: Create package.json**

```json
{
  "name": "radar",
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "@google/generative-ai": "^0.21.0",
    "axios": "^1.7.0",
    "better-sqlite3": "^9.6.0",
    "dotenv": "^16.4.0",
    "imapflow": "^1.0.169",
    "node-cron": "^3.0.3",
    "node-telegram-bot-api": "^0.66.0",
    "nodemailer": "^6.9.0",
    "bcrypt": "^5.1.1",
    "express": "^4.19.0",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "vitest": "^1.6.0"
  }
}
```

- [ ] **Step 2: Install**

Run: `npm install`
Expected: `node_modules/` created, no errors.

- [ ] **Step 3: Commit**

```bash
git add package.json package-lock.json
git commit -m "chore: add package.json with all deps"
```

---

### Task 1.2: db/schema.sql

- [ ] **Step 1: Create db/schema.sql**

```sql
CREATE TABLE IF NOT EXISTS leads (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  company TEXT,
  website TEXT,
  contact_name TEXT,
  contact_email TEXT UNIQUE,
  niche TEXT,
  city TEXT,
  cms TEXT,
  business_signals TEXT,
  quality_score INTEGER,
  icp_score INTEGER,
  icp_priority TEXT,
  hook TEXT,
  email_subject TEXT,
  email_body TEXT,
  status TEXT DEFAULT 'new',
  gemini_cost_usd REAL DEFAULT 0,
  hook_cost_usd REAL DEFAULT 0,
  body_cost_usd REAL DEFAULT 0,
  created_at TEXT DEFAULT (datetime('now')),
  updated_at TEXT DEFAULT (datetime('now'))
);

CREATE TABLE IF NOT EXISTS emails (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  lead_id INTEGER REFERENCES leads(id),
  sequence_step INTEGER DEFAULT 0,
  inbox TEXT,
  subject TEXT,
  body TEXT,
  message_id TEXT,
  status TEXT DEFAULT 'pending',
  sent_at TEXT,
  ai_cost_usd REAL DEFAULT 0,
  created_at TEXT DEFAULT (datetime('now'))
);

CREATE TABLE IF NOT EXISTS bounces (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  email_id INTEGER REFERENCES emails(id),
  lead_id INTEGER REFERENCES leads(id),
  bounce_type TEXT,
  raw_error TEXT,
  created_at TEXT DEFAULT (datetime('now'))
);

CREATE TABLE IF NOT EXISTS replies (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  lead_id INTEGER REFERENCES leads(id),
  email_id INTEGER REFERENCES emails(id),
  inbox TEXT,
  subject TEXT,
  body TEXT,
  classification TEXT,
  classify_cost_usd REAL DEFAULT 0,
  received_at TEXT DEFAULT (datetime('now'))
);

CREATE TABLE IF NOT EXISTS reject_list (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  email TEXT UNIQUE,
  domain TEXT,
  reason TEXT,
  created_at TEXT DEFAULT (datetime('now'))
);

CREATE TABLE IF NOT EXISTS cron_log (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  job_name TEXT,
  started_at TEXT,
  finished_at TEXT,
  status TEXT,
  leads_found INTEGER DEFAULT 0,
  emails_sent INTEGER DEFAULT 0,
  cost_usd REAL DEFAULT 0,
  error TEXT
);

CREATE TABLE IF NOT EXISTS daily_metrics (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  date TEXT UNIQUE,
  leads_found INTEGER DEFAULT 0,
  emails_sent INTEGER DEFAULT 0,
  bounces INTEGER DEFAULT 0,
  replies INTEGER DEFAULT 0,
  hot_replies INTEGER DEFAULT 0,
  gemini_cost_usd REAL DEFAULT 0,
  sonnet_cost_usd REAL DEFAULT 0,
  haiku_cost_usd REAL DEFAULT 0,
  total_cost_usd REAL DEFAULT 0
);

CREATE TABLE IF NOT EXISTS error_log (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  source TEXT,
  message TEXT,
  stack TEXT,
  resolved INTEGER DEFAULT 0,
  created_at TEXT DEFAULT (datetime('now'))
);

CREATE TABLE IF NOT EXISTS sequence_state (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  lead_id INTEGER UNIQUE REFERENCES leads(id),
  current_step INTEGER DEFAULT 0,
  next_send_at TEXT,
  last_message_id TEXT,
  last_references TEXT,
  status TEXT DEFAULT 'active',
  updated_at TEXT DEFAULT (datetime('now'))
);

CREATE INDEX IF NOT EXISTS idx_leads_status ON leads(status);
CREATE INDEX IF NOT EXISTS idx_leads_icp_priority ON leads(icp_priority);
CREATE INDEX IF NOT EXISTS idx_leads_contact_email ON leads(contact_email);
CREATE INDEX IF NOT EXISTS idx_emails_sent_at ON emails(sent_at);
CREATE INDEX IF NOT EXISTS idx_emails_status ON emails(status);
CREATE INDEX IF NOT EXISTS idx_replies_lead_id ON replies(lead_id);
CREATE INDEX IF NOT EXISTS idx_cron_log_job_name ON cron_log(job_name);
CREATE INDEX IF NOT EXISTS idx_daily_metrics_date ON daily_metrics(date);
CREATE INDEX IF NOT EXISTS idx_error_log_source ON error_log(source);
CREATE INDEX IF NOT EXISTS idx_reject_list_email ON reject_list(email);
CREATE INDEX IF NOT EXISTS idx_reject_list_domain ON reject_list(domain);
```

- [ ] **Step 2: Commit**

```bash
git add db/schema.sql
git commit -m "feat: add SQLite schema (9 tables + indices)"
```

---

### Task 1.3: utils/db.js

- [ ] **Step 1: Write failing test** — `tests/utils/db.test.js`

```js
import { describe, it, expect, beforeEach, afterEach } from 'vitest';
import { mkdtempSync, rmSync } from 'fs';
import { tmpdir } from 'os';
import { join } from 'path';

let tmpDir;

beforeEach(async () => {
  tmpDir = mkdtempSync(join(tmpdir(), 'radar-test-'));
  process.env.DB_PATH = join(tmpDir, 'radar.sqlite');
  const { resetDb, initSchema } = await import('../../utils/db.js');
  resetDb(); // close any prior singleton so DB_PATH change takes effect
  initSchema();
});

afterEach(async () => {
  const { resetDb } = await import('../../utils/db.js');
  resetDb();
  rmSync(tmpDir, { recursive: true });
});

describe('db helpers', () => {
  it('today() returns YYYY-MM-DD', async () => {
    const { today } = await import('../../utils/db.js');
    expect(today()).toMatch(/^\d{4}-\d{2}-\d{2}$/);
  });

  it('logError inserts into error_log', async () => {
    const { logError, getDb } = await import('../../utils/db.js');
    logError('test-source', new Error('boom'));
    const row = getDb().prepare('SELECT * FROM error_log WHERE source=?').get('test-source');
    expect(row).toBeTruthy();
    expect(row.message).toBe('boom');
  });

  it('isRejected returns false for unknown email', async () => {
    const { isRejected } = await import('../../utils/db.js');
    expect(isRejected('nobody@example.com')).toBe(false);
  });

  it('addToRejectList + isRejected roundtrip', async () => {
    const { addToRejectList, isRejected } = await import('../../utils/db.js');
    addToRejectList('test@spam.com', 'unsubscribe');
    expect(isRejected('test@spam.com')).toBe(true);
  });
});
```

- [ ] **Step 2: Run test — expect FAIL**

Run: `npx vitest run tests/utils/db.test.js`
Expected: FAIL (utils/db.js does not exist)

- [ ] **Step 3: Implement utils/db.js**

```js
import Database from 'better-sqlite3';
import { readFileSync } from 'fs';
import { fileURLToPath } from 'url';
import { join, dirname } from 'path';
import 'dotenv/config';

const __dirname = dirname(fileURLToPath(import.meta.url));

let _db;

export function getDb() {
  if (!_db) {
    _db = new Database(process.env.DB_PATH || '/home/radar/db/radar.sqlite');
    _db.pragma('journal_mode = WAL');
    _db.pragma('foreign_keys = ON');
  }
  return _db;
}

/** For tests only — close and reset the singleton so DB_PATH changes take effect */
export function resetDb() {
  if (_db) { _db.close(); _db = null; }
}

export function initSchema() {
  const sql = readFileSync(join(__dirname, '../db/schema.sql'), 'utf8');
  getDb().exec(sql);
}

export function today() {
  return new Date().toISOString().slice(0, 10);
}

export function bumpMetric(field, amount = 1) {
  const db = getDb();
  const d = today();
  db.prepare(`INSERT INTO daily_metrics (date) VALUES (?) ON CONFLICT(date) DO NOTHING`).run(d);
  db.prepare(`UPDATE daily_metrics SET ${field} = ${field} + ? WHERE date = ?`).run(amount, d);
}

export function logError(source, err) {
  getDb().prepare(
    `INSERT INTO error_log (source, message, stack) VALUES (?, ?, ?)`
  ).run(source, err.message || String(err), err.stack || null);
}

export function logCron(jobName) {
  const info = getDb().prepare(
    `INSERT INTO cron_log (job_name, started_at, status) VALUES (?, datetime('now'), 'running') RETURNING id`
  ).get(jobName);
  return info.id;
}

export function finishCron(id, { status = 'ok', leadsFound = 0, emailsSent = 0, costUsd = 0, error = null } = {}) {
  getDb().prepare(
    `UPDATE cron_log SET finished_at=datetime('now'), status=?, leads_found=?, emails_sent=?, cost_usd=?, error=? WHERE id=?`
  ).run(status, leadsFound, emailsSent, costUsd, error, id);
}

export function isRejected(email) {
  const domain = email.split('@')[1];
  const row = getDb().prepare(
    `SELECT 1 FROM reject_list WHERE email=? OR domain=? LIMIT 1`
  ).get(email, domain);
  return !!row;
}

export function addToRejectList(email, reason) {
  const domain = email.split('@')[1];
  getDb().prepare(
    `INSERT OR IGNORE INTO reject_list (email, domain, reason) VALUES (?, ?, ?)`
  ).run(email, domain, reason);
}

export function todaySentCount() {
  const row = getDb().prepare(
    `SELECT emails_sent FROM daily_metrics WHERE date=?`
  ).get(today());
  return row?.emails_sent || 0;
}

export function todayBounceRate() {
  const row = getDb().prepare(
    `SELECT emails_sent, bounces FROM daily_metrics WHERE date=?`
  ).get(today());
  if (!row || row.emails_sent === 0) return 0;
  return row.bounces / row.emails_sent;
}
```

- [ ] **Step 4: Run test — expect PASS**

Run: `npx vitest run tests/utils/db.test.js`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add utils/db.js tests/utils/db.test.js
git commit -m "feat: add utils/db.js with schema init and query helpers"
```

---

### Task 1.4: utils/sleep.js

- [ ] **Step 1: Write failing test** — `tests/utils/sleep.test.js`

```js
import { describe, it, expect } from 'vitest';
import { sleep } from '../../utils/sleep.js';

describe('sleep', () => {
  it('resolves after at least minMs', async () => {
    const start = Date.now();
    await sleep(10, 20);
    expect(Date.now() - start).toBeGreaterThanOrEqual(10);
  });
});
```

- [ ] **Step 2: Run test — expect FAIL**

Run: `npx vitest run tests/utils/sleep.test.js`
Expected: FAIL

- [ ] **Step 3: Implement utils/sleep.js**

```js
export function sleep(minMs, maxMs) {
  const ms = minMs + Math.random() * (maxMs - minMs);
  return new Promise(resolve => setTimeout(resolve, ms));
}
```

- [ ] **Step 4: Run test — expect PASS**

Run: `npx vitest run tests/utils/sleep.test.js`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add utils/sleep.js tests/utils/sleep.test.js
git commit -m "feat: add utils/sleep.js"
```

---

### Task 1.5: utils/telegram.js

- [ ] **Step 1: Write failing test** — `tests/utils/telegram.test.js`

```js
import { describe, it, expect, vi } from 'vitest';

describe('telegram stub', () => {
  it('sendAlert does not throw when token absent', async () => {
    delete process.env.TELEGRAM_BOT_TOKEN;
    const { sendAlert } = await import('../../utils/telegram.js');
    await expect(sendAlert('test message')).resolves.not.toThrow();
  });
});
```

- [ ] **Step 2: Run test — expect FAIL**

Run: `npx vitest run tests/utils/telegram.test.js`
Expected: FAIL

- [ ] **Step 3: Implement utils/telegram.js**

```js
import TelegramBot from 'node-telegram-bot-api';
import 'dotenv/config';

let _bot;

function getBot() {
  if (!process.env.TELEGRAM_BOT_TOKEN) return null;
  if (!_bot) _bot = new TelegramBot(process.env.TELEGRAM_BOT_TOKEN);
  return _bot;
}

export async function sendAlert(message) {
  const bot = getBot();
  if (!bot || !process.env.TELEGRAM_CHAT_ID) {
    console.log('[Telegram stub]', message);
    return;
  }
  await bot.sendMessage(process.env.TELEGRAM_CHAT_ID, message);
}
```

- [ ] **Step 4: Run test — expect PASS**

Run: `npx vitest run tests/utils/telegram.test.js`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add utils/telegram.js tests/utils/telegram.test.js
git commit -m "feat: add utils/telegram.js with graceful stub"
```

---

### Task 1.6: ecosystem.config.js + cron.js skeleton

- [ ] **Step 1: Create ecosystem.config.js**

```js
export default {
  apps: [
    {
      name: 'radar-cron',
      script: './cron.js',
      instances: 1,
      autorestart: true,
      watch: false,
      max_memory_restart: '300M',
      env: { NODE_ENV: 'production' }
    },
    {
      name: 'radar-dashboard',
      script: './dashboard/server.js',
      instances: 1,
      autorestart: true,
      watch: false,
      max_memory_restart: '300M',
      env: { NODE_ENV: 'production' }
    }
  ]
};
```

- [ ] **Step 2: Create cron.js skeleton**

```js
import cron from 'node-cron';
import 'dotenv/config';
import { initSchema } from './utils/db.js';

initSchema();

// 8:00 AM IST Mon–Sat — Find leads
cron.schedule('0 8 * * 1-6', () => {
  import('./findLeads.js').then(m => m.default()).catch(console.error);
}, { timezone: 'Asia/Kolkata' });

// 9:30 AM IST Mon–Sat — Send cold emails
cron.schedule('30 9 * * 1-6', () => {
  import('./sendEmails.js').then(m => m.default()).catch(console.error);
}, { timezone: 'Asia/Kolkata' });

// 2:00 PM IST daily — Check replies
cron.schedule('0 14 * * *', () => {
  import('./checkReplies.js').then(m => m.default()).catch(console.error);
}, { timezone: 'Asia/Kolkata' });

// 4:00 PM IST daily — Check replies
cron.schedule('0 16 * * *', () => {
  import('./checkReplies.js').then(m => m.default()).catch(console.error);
}, { timezone: 'Asia/Kolkata' });

// 6:00 PM IST Mon–Sat — Send follow-ups
cron.schedule('0 18 * * 1-6', () => {
  import('./sendFollowups.js').then(m => m.default()).catch(console.error);
}, { timezone: 'Asia/Kolkata' });

// 8:00 PM IST daily — Check replies
cron.schedule('0 20 * * *', () => {
  import('./checkReplies.js').then(m => m.default()).catch(console.error);
}, { timezone: 'Asia/Kolkata' });

// 8:30 PM IST daily — Daily report
cron.schedule('30 20 * * *', () => {
  import('./dailyReport.js').then(m => m.default()).catch(console.error);
}, { timezone: 'Asia/Kolkata' });

// 2:00 AM IST Sunday — Health check
cron.schedule('0 2 * * 0', () => {
  import('./healthCheck.js').then(m => m.default()).catch(console.error);
}, { timezone: 'Asia/Kolkata' });

// Midnight IST daily — Backup
cron.schedule('0 0 * * *', () => {
  import('child_process').then(({ exec }) => {
    exec('./backup.sh', (err) => {
      if (err) {
        import('./utils/db.js').then(({ logError }) => logError('backup', err));
      }
    });
  });
}, { timezone: 'Asia/Kolkata' });

console.log('Radar cron started');
```

- [ ] **Step 3: Commit**

```bash
git add ecosystem.config.js cron.js
git commit -m "feat: add ecosystem.config.js and cron.js skeleton"
```

---

### Task 1.7: Run all Chunk 1 tests

- [ ] **Step 1: Run full test suite**

Run: `npx vitest run`
Expected: All tests PASS.

- [ ] **Step 2: Commit if any fixes needed, then tag chunk complete**

```bash
git add -A
git commit -m "chore: chunk 1 foundation complete"
```

---

## Chunk 2: Engine 1 — AI Clients

**Files:**
- Create: `utils/gemini.js`
- Create: `utils/claude.js`
- Create: `utils/mev.js`
- Create: `tests/utils/gemini.test.js`
- Create: `tests/utils/claude.test.js`
- Create: `tests/utils/mev.test.js`

---

### Task 2.1: utils/gemini.js

- [ ] **Step 1: Write failing test** — `tests/utils/gemini.test.js`

```js
import { describe, it, expect, vi } from 'vitest';

vi.mock('@google/generative-ai', () => ({
  GoogleGenerativeAI: vi.fn(() => ({
    getGenerativeModel: vi.fn(() => ({
      generateContent: vi.fn(async () => ({
        response: { text: () => 'mock response', usageMetadata: { promptTokenCount: 10, candidatesTokenCount: 20 } }
      }))
    }))
  }))
}));

describe('gemini client', () => {
  it('callGemini returns text and cost', async () => {
    process.env.GEMINI_API_KEY = 'test-key';
    process.env.GEMINI_MODEL = 'gemini-2.5-flash';
    const { callGemini } = await import('../../utils/gemini.js');
    const result = await callGemini('test prompt');
    expect(result.text).toBe('mock response');
    expect(typeof result.costUsd).toBe('number');
  });
});
```

- [ ] **Step 2: Run test — expect FAIL**

Run: `npx vitest run tests/utils/gemini.test.js`

- [ ] **Step 3: Implement utils/gemini.js**

```js
import { GoogleGenerativeAI } from '@google/generative-ai';
import 'dotenv/config';

// Gemini 2.5 Flash pricing (per 1M tokens, non-thinking)
const INPUT_COST_PER_1M = 0.15;
const OUTPUT_COST_PER_1M = 0.60;

let _client;

function getClient() {
  if (!_client) _client = new GoogleGenerativeAI(process.env.GEMINI_API_KEY);
  return _client;
}

/**
 * @param {string} prompt
 * @param {{ useGrounding?: boolean }} opts
 * @returns {Promise<{ text: string, costUsd: number, inputTokens: number, outputTokens: number }>}
 */
export async function callGemini(prompt, { useGrounding = false } = {}) {
  const model = getClient().getGenerativeModel({
    model: process.env.GEMINI_MODEL || 'gemini-2.5-flash',
    ...(useGrounding ? { tools: [{ googleSearch: {} }] } : {})
  });

  const result = await model.generateContent(prompt);
  const response = result.response;
  const text = response.text();
  const usage = response.usageMetadata || {};
  const inputTokens = usage.promptTokenCount || 0;
  const outputTokens = usage.candidatesTokenCount || 0;
  const costUsd = (inputTokens / 1_000_000) * INPUT_COST_PER_1M
                + (outputTokens / 1_000_000) * OUTPUT_COST_PER_1M;

  return { text, costUsd, inputTokens, outputTokens };
}
```

- [ ] **Step 4: Run test — expect PASS**

Run: `npx vitest run tests/utils/gemini.test.js`

- [ ] **Step 5: Commit**

```bash
git add utils/gemini.js tests/utils/gemini.test.js
git commit -m "feat: add utils/gemini.js with cost tracking"
```

---

### Task 2.2: utils/claude.js

- [ ] **Step 1: Write failing test** — `tests/utils/claude.test.js`

```js
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { mkdtempSync, rmSync } from 'fs';
import { tmpdir } from 'os';
import { join } from 'path';

vi.mock('@anthropic-ai/sdk', () => ({
  default: vi.fn(() => ({
    messages: {
      create: vi.fn(async () => ({
        content: [{ text: 'mock claude response' }],
        usage: { input_tokens: 100, output_tokens: 50 }
      }))
    }
  }))
}));

let tmpDir;
beforeEach(async () => {
  tmpDir = mkdtempSync(join(tmpdir(), 'radar-test-'));
  process.env.DB_PATH = join(tmpDir, 'radar.sqlite');
  process.env.CLAUDE_DAILY_SPEND_CAP = '3.00';
  const { resetDb, initSchema } = await import('../../utils/db.js');
  resetDb();
  initSchema();
});

afterEach(async () => {
  const { resetDb } = await import('../../utils/db.js');
  resetDb();
  rmSync(tmpDir, { recursive: true });
});

describe('claude client', () => {
  it('callClaude returns text and cost', async () => {
    process.env.ANTHROPIC_API_KEY = 'test-key';
    const { callClaude } = await import('../../utils/claude.js');
    const result = await callClaude('sonnet', 'test prompt');
    expect(result.text).toBe('mock claude response');
    expect(typeof result.costUsd).toBe('number');
  });

  it('throws when daily spend cap exceeded', async () => {
    process.env.CLAUDE_DAILY_SPEND_CAP = '0.00';
    // Simulate spend already at cap by inserting a daily_metrics row
    const { getDb, today } = await import('../../utils/db.js');
    getDb().prepare(
      `INSERT INTO daily_metrics (date, sonnet_cost_usd, haiku_cost_usd) VALUES (?, 0.01, 0.01)`
    ).run(today());
    const { callClaude } = await import('../../utils/claude.js');
    await expect(callClaude('haiku', 'test')).rejects.toThrow(/spend cap/i);
  });
});
```

- [ ] **Step 2: Run test — expect FAIL**

Run: `npx vitest run tests/utils/claude.test.js`

- [ ] **Step 3: Implement utils/claude.js**

```js
import Anthropic from '@anthropic-ai/sdk';
import 'dotenv/config';
import { getDb, today, logError } from './db.js';

// Pricing per 1M tokens
const PRICING = {
  sonnet: { input: 3.00, output: 15.00 },
  haiku:  { input: 0.80, output: 4.00  }
};

const MODEL_IDS = {
  sonnet: 'claude-sonnet-4-20250514',
  haiku:  'claude-haiku-4-5-20251001'
};

let _client;
function getClient() {
  if (!_client) _client = new Anthropic({ apiKey: process.env.ANTHROPIC_API_KEY });
  return _client;
}

async function checkSpendCap() {
  const cap = parseFloat(process.env.CLAUDE_DAILY_SPEND_CAP || '3.00');
  const row = getDb().prepare(
    `SELECT sonnet_cost_usd, haiku_cost_usd FROM daily_metrics WHERE date=?`
  ).get(today());
  const spent = (row?.sonnet_cost_usd || 0) + (row?.haiku_cost_usd || 0);
  if (spent >= cap) {
    const err = new Error(`Claude daily spend cap ($${cap}) reached — spent $${spent.toFixed(4)}`);
    logError('claude.spendCap', err);
    throw err;
  }
}

/**
 * @param {'sonnet'|'haiku'} model
 * @param {string} prompt
 * @param {{ systemPrompt?: string, maxTokens?: number }} opts
 */
export async function callClaude(model, prompt, { systemPrompt, maxTokens = 1024 } = {}) {
  await checkSpendCap();

  const pricing = PRICING[model];
  if (!pricing) throw new Error(`Unknown model alias: ${model}`);

  const messages = [{ role: 'user', content: prompt }];
  const params = {
    model: MODEL_IDS[model],
    max_tokens: maxTokens,
    messages,
    ...(systemPrompt ? { system: systemPrompt } : {})
  };

  const response = await getClient().messages.create(params);
  const text = response.content[0].text;
  const inputTokens = response.usage.input_tokens;
  const outputTokens = response.usage.output_tokens;
  const costUsd = (inputTokens / 1_000_000) * pricing.input
                + (outputTokens / 1_000_000) * pricing.output;

  // Write cost to daily_metrics
  const db = getDb();
  const d = today();
  db.prepare(`INSERT INTO daily_metrics (date) VALUES (?) ON CONFLICT(date) DO NOTHING`).run(d);
  const col = model === 'sonnet' ? 'sonnet_cost_usd' : 'haiku_cost_usd';
  db.prepare(`UPDATE daily_metrics SET ${col}=${col}+?, total_cost_usd=total_cost_usd+? WHERE date=?`)
    .run(costUsd, costUsd, d);

  return { text, costUsd, inputTokens, outputTokens };
}
```

- [ ] **Step 4: Run test — expect PASS**

Run: `npx vitest run tests/utils/claude.test.js`

- [ ] **Step 5: Commit**

```bash
git add utils/claude.js tests/utils/claude.test.js
git commit -m "feat: add utils/claude.js with spend cap guard"
```

---

### Task 2.3: utils/mev.js

- [ ] **Step 1: Write failing test** — `tests/utils/mev.test.js`

```js
import { describe, it, expect, vi } from 'vitest';

vi.mock('axios', () => ({
  default: {
    get: vi.fn(async () => ({
      data: { status: 'valid', free_email: false, disposable: false, score: 0.9 }
    }))
  }
}));

describe('mev client', () => {
  it('verifyEmail returns status and confidence', async () => {
    process.env.MEV_API_KEY = 'test-key';
    const { verifyEmail } = await import('../../utils/mev.js');
    const result = await verifyEmail('test@example.com');
    expect(result.status).toBe('valid');
    expect(typeof result.confidence).toBe('number');
  });

  it('returns invalid when no API key', async () => {
    delete process.env.MEV_API_KEY;
    const { verifyEmail } = await import('../../utils/mev.js');
    const result = await verifyEmail('test@example.com');
    expect(result.status).toBe('skipped');
  });
});
```

- [ ] **Step 2: Run test — expect FAIL**

Run: `npx vitest run tests/utils/mev.test.js`

- [ ] **Step 3: Implement utils/mev.js**

```js
import axios from 'axios';
import 'dotenv/config';

const MEV_BASE = 'https://api.myemailverifier.com/verify';

/**
 * @param {string} email
 * @returns {Promise<{ status: string, confidence: number }>}
 */
export async function verifyEmail(email) {
  if (!process.env.MEV_API_KEY) {
    return { status: 'skipped', confidence: 0 };
  }
  try {
    const { data } = await axios.get(MEV_BASE, {
      params: { secret: process.env.MEV_API_KEY, email },
      timeout: 10000
    });
    // status: 'valid' | 'invalid' | 'disposable' | 'unknown'
    const confidence = data.score ?? (data.status === 'valid' ? 0.9 : 0);
    return { status: data.status, confidence };
  } catch (err) {
    return { status: 'error', confidence: 0 };
  }
}
```

- [ ] **Step 4: Run test — expect PASS**

Run: `npx vitest run tests/utils/mev.test.js`

- [ ] **Step 5: Commit**

```bash
git add utils/mev.js tests/utils/mev.test.js
git commit -m "feat: add utils/mev.js email verifier client"
```

---

### Task 2.4: Run all Chunk 2 tests

- [ ] **Step 1:** Run: `npx vitest run` — Expected: All tests PASS.

- [ ] **Step 2: Commit**

```bash
git commit -m "chore: chunk 2 AI clients complete" --allow-empty
```

---

## Chunk 3: Engine 1 — findLeads.js (11-stage pipeline)

**Files:**
- Create: `findLeads.js`
- Create: `tests/findLeads.test.js`

The 11-stage pipeline: Stages 1–6 + 9 use Gemini (grounding); Stage 7 MEV; Stage 8 SQLite dedup; Stage 10 Claude Sonnet (hook); Stage 11 Claude Haiku (body).

---

### Task 3.1: findLeads.js

- [ ] **Step 1: Write failing test** — `tests/findLeads.test.js`

```js
import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
import { mkdtempSync, rmSync } from 'fs';
import { tmpdir } from 'os';
import { join } from 'path';

vi.mock('../utils/gemini.js', () => ({
  callGemini: vi.fn(async (prompt) => {
    if (prompt.includes('discover')) {
      return { text: JSON.stringify([{ company: 'Acme', website: 'https://acme.com', city: 'Mumbai', niche: 'restaurant' }]), costUsd: 0.001, inputTokens: 100, outputTokens: 50 };
    }
    if (prompt.includes('extract')) {
      return { text: JSON.stringify({ contact_name: 'John', cms: 'WordPress', business_signals: 'low reviews', quality_score: 8, contact_email: 'john@acme.com' }), costUsd: 0.001, inputTokens: 100, outputTokens: 50 };
    }
    if (prompt.includes('icp')) {
      return { text: JSON.stringify({ icp_score: 7, icp_priority: 'A' }), costUsd: 0.001, inputTokens: 50, outputTokens: 20 };
    }
    return { text: '{}', costUsd: 0, inputTokens: 0, outputTokens: 0 };
  })
}));

vi.mock('../utils/claude.js', () => ({
  callClaude: vi.fn(async () => ({ text: 'Your site looks dated.', costUsd: 0.002, inputTokens: 200, outputTokens: 30 }))
}));

vi.mock('../utils/mev.js', () => ({
  verifyEmail: vi.fn(async () => ({ status: 'valid', confidence: 0.9 }))
}));

let tmpDir;
beforeEach(async () => {
  tmpDir = mkdtempSync(join(tmpdir(), 'radar-test-'));
  process.env.DB_PATH = join(tmpDir, 'radar.sqlite');
  const { resetDb, initSchema } = await import('../utils/db.js');
  resetDb();
  initSchema();
});

afterEach(async () => {
  const { resetDb } = await import('../utils/db.js');
  resetDb();
  rmSync(tmpDir, { recursive: true });
});

describe('findLeads', () => {
  it('runs pipeline and inserts ready leads', async () => {
    const findLeads = (await import('../findLeads.js')).default;
    await findLeads();
    const { getDb } = await import('../utils/db.js');
    const leads = getDb().prepare(`SELECT * FROM leads WHERE status='ready'`).all();
    expect(leads.length).toBeGreaterThan(0);
    expect(leads[0].hook).toBeTruthy();
    expect(leads[0].email_body).toBeTruthy();
  });
});
```

- [ ] **Step 2: Run test — expect FAIL**

Run: `npx vitest run tests/findLeads.test.js`

- [ ] **Step 3: Implement findLeads.js**

```js
import 'dotenv/config';
import { getDb, logCron, finishCron, logError, bumpMetric, isRejected, today } from './utils/db.js';
import { callGemini } from './utils/gemini.js';
import { callClaude } from './utils/claude.js';
import { verifyEmail } from './utils/mev.js';
import { sendAlert } from './utils/telegram.js';

const NICHES = {
  1: { label: 'Shopify/D2C', query: 'India D2C ecommerce brand Shopify outdated website' },
  2: { label: 'Real estate', query: 'Mumbai real estate agency property portal outdated website' },
  3: { label: 'Funded startups', query: 'India funded B2B startup outdated website developer needed' },
  4: { label: 'Restaurants/cafes', query: 'Mumbai restaurant cafe outdated website no online booking' },
  5: { label: 'Agencies', query: 'Mumbai digital agency overflow web development outsource' },
  6: { label: 'Healthcare/salons', query: 'India healthcare salon clinic outdated website no booking' }
};

function getNicheForToday() {
  const dow = new Date().getDay(); // 0=Sun,1=Mon,...,6=Sat
  return NICHES[dow] || NICHES[1];
}

async function stage1_discover(niche) {
  const prompt = `You are a B2B lead researcher. discover 20 real Indian businesses in the "${niche.label}" niche that likely have outdated websites. Search query context: "${niche.query}". Return a JSON array of objects: [{company, website, city, niche}]. Return only valid JSON, no markdown.`;
  const result = await callGemini(prompt, { useGrounding: true });
  try {
    return { leads: JSON.parse(result.text), costUsd: result.costUsd };
  } catch {
    return { leads: [], costUsd: result.costUsd };
  }
}

async function stages2to6_extract(lead) {
  const prompt = `Analyze this business website and return a JSON object with these fields:
- contact_name: owner/founder name (string or null)
- contact_email: guessed email from name + domain pattern (string or null)
- cms: CMS detected from meta/scripts (string or null)
- business_signals: comma-separated signals like "low reviews,no booking,dated design" (string)
- quality_score: 1-10 how likely they need web help (number)

Business: ${lead.company}, Website: ${lead.website}, City: ${lead.city}

Return only valid JSON, no markdown.`;
  const result = await callGemini(prompt, { useGrounding: true });
  try {
    return { data: JSON.parse(result.text), costUsd: result.costUsd };
  } catch {
    return { data: null, costUsd: result.costUsd };
  }
}

async function stage9_icpScore(lead) {
  const prompt = `Score this lead on the ICP rubric and return JSON {icp_score: number, icp_priority: "A"|"B"|"C"}.

Rubric:
+3  India-based B2C-facing
+2  20+ Google reviews
+2  WordPress/Wix/Squarespace stack
+2  Website last updated 2+ years ago
+1  Active social but neglected website
+1  WhatsApp Business but no online booking
-2  Freelancer/solo consultant
-3  Modern stack (Next.js/Webflow/custom React)

Lead data:
Company: ${lead.company}
CMS: ${lead.cms || 'unknown'}
Business signals: ${lead.business_signals || 'none'}
City: ${lead.city}
Niche: ${lead.niche}

Return only valid JSON.`;
  const result = await callGemini(prompt);
  try {
    return { data: JSON.parse(result.text), costUsd: result.costUsd };
  } catch {
    return { data: { icp_score: 0, icp_priority: 'C' }, costUsd: result.costUsd };
  }
}

async function stage10_hook(lead) {
  const result = await callClaude('sonnet',
    `Write ONE sentence (max 20 words) that makes a hyper-specific observation about ${lead.company}'s website (${lead.website}). Focus on something concrete you'd notice as a developer — outdated tech, missing feature, design issue. No fluff, no compliments.`,
    { maxTokens: 60 }
  );
  return { hook: result.text.trim(), costUsd: result.costUsd };
}

async function stage11_body(lead) {
  const result = await callClaude('haiku',
    `Write a cold email from Darshan Parmar (Full-Stack Developer, Simple Inc) to ${lead.contact_name || 'the owner'} at ${lead.company}.

Hook to open with: "${lead.hook}"

Rules:
- Plain text only, no HTML
- 50–90 words total
- No links, no URLs
- CTA: ask to reply
- Professional but direct tone
- Do not mention price

Return only the email body, no subject line.`,
    { maxTokens: 200 }
  );
  return { body: result.text.trim(), costUsd: result.costUsd };
}

async function stage11_subject(lead) {
  const result = await callClaude('haiku',
    `Write a cold email subject line for ${lead.company}. Max 7 words. No ! or ? or ALL CAPS. Make it sound like a human colleague writing, not marketing. Return only the subject line text.`,
    { maxTokens: 30 }
  );
  return { subject: result.text.trim(), costUsd: result.costUsd };
}

export default async function findLeads() {
  const cronId = logCron('findLeads');
  let totalCost = 0;
  let leadsReady = 0;

  try {
    const niche = getNicheForToday();
    const { leads: rawLeads, costUsd: c1 } = await stage1_discover(niche);
    totalCost += c1;

    for (const raw of rawLeads) {
      try {
        // Stage 2–6: Extract
        const { data: extracted, costUsd: c2 } = await stages2to6_extract(raw);
        totalCost += c2;
        if (!extracted || !extracted.contact_email) continue;
        if (extracted.quality_score < 6) continue; // Gate 1

        const lead = { ...raw, ...extracted };

        // Stage 7: Email verify
        const { status: verifyStatus, confidence } = await verifyEmail(lead.contact_email);
        if (verifyStatus === 'invalid' || verifyStatus === 'disposable') continue;
        if (verifyStatus === 'unknown' && confidence < 0.5) continue; // Gate 2

        // Stage 8: Dedup
        if (isRejected(lead.contact_email)) continue;
        const existing = getDb().prepare(`SELECT id FROM leads WHERE contact_email=?`).get(lead.contact_email);
        if (existing) continue;

        // Stage 9: ICP score
        const { data: icp, costUsd: c9 } = await stage9_icpScore(lead);
        totalCost += c9;
        lead.icp_score = icp.icp_score;
        lead.icp_priority = icp.icp_priority;

        // Gate 3: C-priority → nurture, not discard
        const finalStatus = icp.icp_priority === 'C' ? 'nurture' : 'ready';

        // Stage 10: Hook (only for A/B)
        let hookCost = 0, hook = null;
        if (finalStatus === 'ready') {
          const h = await stage10_hook(lead);
          hook = h.hook;
          hookCost = h.costUsd;
          totalCost += hookCost;
        }

        // Stage 11: Email body + subject (only for A/B)
        let bodyCost = 0, emailBody = null, emailSubject = null;
        if (finalStatus === 'ready' && hook) {
          lead.hook = hook;
          const [bodyRes, subjectRes] = await Promise.all([
            stage11_body(lead),
            stage11_subject(lead)
          ]);
          emailBody = bodyRes.body;
          emailSubject = subjectRes.subject;
          bodyCost = bodyRes.costUsd + subjectRes.costUsd;
          totalCost += bodyCost;
        }

        const geminiCost = c2 + c9;

        // Insert lead
        getDb().prepare(`
          INSERT INTO leads (company, website, contact_name, contact_email, niche, city, cms,
            business_signals, quality_score, icp_score, icp_priority, hook,
            email_subject, email_body, status, gemini_cost_usd, hook_cost_usd, body_cost_usd)
          VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)
        `).run(
          lead.company, lead.website, lead.contact_name, lead.contact_email,
          lead.niche, lead.city, lead.cms, lead.business_signals,
          lead.quality_score, lead.icp_score, lead.icp_priority, hook,
          emailSubject, emailBody, finalStatus,
          geminiCost, hookCost, bodyCost
        );

        bumpMetric('leads_found');
        bumpMetric('gemini_cost_usd', c2 + c9);
        if (hookCost > 0) bumpMetric('sonnet_cost_usd', hookCost);
        if (bodyCost > 0) bumpMetric('haiku_cost_usd', bodyCost);

        if (finalStatus === 'ready') leadsReady++;
      } catch (leadErr) {
        logError('findLeads.lead', leadErr);
      }
    }

    finishCron(cronId, { status: 'ok', leadsFound: leadsReady, costUsd: totalCost });
    await sendAlert(`✅ findLeads: ${leadsReady} leads ready (cost $${totalCost.toFixed(4)})`);
  } catch (err) {
    logError('findLeads', err);
    finishCron(cronId, { status: 'error', error: err.message });
    await sendAlert(`🚨 findLeads failed: ${err.message}`);
  }
}
```

- [ ] **Step 4: Run test — expect PASS**

Run: `npx vitest run tests/findLeads.test.js`

- [ ] **Step 5: Commit**

```bash
git add findLeads.js tests/findLeads.test.js
git commit -m "feat: add findLeads.js — 11-stage lead intelligence pipeline"
```

---

### Task 3.2: Run all Chunk 3 tests

- [ ] **Step 1:** Run: `npx vitest run` — Expected: All tests PASS.

- [ ] **Step 2: Commit**

```bash
git commit -m "chore: chunk 3 Engine 1 findLeads complete" --allow-empty
```

---

## Chunk 4: Anti-Spam Engine

**Files:**
- Create: `utils/contentValidator.js`
- Create: `utils/blacklistCheck.js`
- Create: `healthCheck.js`
- Create: `tests/utils/contentValidator.test.js`
- Create: `tests/utils/blacklistCheck.test.js`

---

### Task 4.1: utils/contentValidator.js

- [ ] **Step 1: Write failing test** — `tests/utils/contentValidator.test.js`

```js
import { describe, it, expect } from 'vitest';
import { validate } from '../../utils/contentValidator.js';

describe('contentValidator', () => {
  const goodSubject = 'Quick question about your site';
  const goodBody = 'Hi John, I noticed your website hasn\'t been updated in a few years. I help businesses like yours modernize their web presence quickly. Would you be open to a quick chat? Reply to this email and we can find a time. Darshan';

  it('passes valid email', () => {
    const result = validate(goodSubject, goodBody, 0);
    expect(result.valid).toBe(true);
  });

  it('fails HTML content', () => {
    const result = validate(goodSubject, '<b>Hello</b> world', 0);
    expect(result.valid).toBe(false);
    expect(result.reason).toMatch(/html/i);
  });

  it('fails body too short', () => {
    const result = validate(goodSubject, 'Too short', 0);
    expect(result.valid).toBe(false);
    expect(result.reason).toMatch(/word count/i);
  });

  it('fails body too long', () => {
    const longBody = Array(100).fill('word').join(' ');
    const result = validate(goodSubject, longBody, 0);
    expect(result.valid).toBe(false);
    expect(result.reason).toMatch(/word count/i);
  });

  it('fails URL in step 0', () => {
    const result = validate(goodSubject, goodBody + ' https://example.com', 0);
    expect(result.valid).toBe(false);
    expect(result.reason).toMatch(/url/i);
  });

  it('allows URL in step 2', () => {
    const result = validate(goodSubject, goodBody + ' https://example.com', 2);
    expect(result.valid).toBe(true);
  });

  it('fails spam word', () => {
    process.env.SPAM_WORDS = 'free,guarantee';
    const result = validate(goodSubject, goodBody + ' This is a free offer', 0);
    expect(result.valid).toBe(false);
    expect(result.reason).toMatch(/spam/i);
  });

  it('fails subject with !', () => {
    const result = validate('Amazing offer!', goodBody, 0);
    expect(result.valid).toBe(false);
    expect(result.reason).toMatch(/subject/i);
  });

  it('fails unfilled template variable', () => {
    const result = validate(goodSubject, 'Hello {{name}}, how are you?', 0);
    expect(result.valid).toBe(false);
    expect(result.reason).toMatch(/unfilled/i);
  });
});
```

- [ ] **Step 2: Run test — expect FAIL**

Run: `npx vitest run tests/utils/contentValidator.test.js`

- [ ] **Step 3: Implement utils/contentValidator.js**

```js
import 'dotenv/config';

const MAX_WORDS = parseInt(process.env.MAX_EMAIL_WORDS || '90');
const MIN_WORDS = parseInt(process.env.MIN_EMAIL_WORDS || '40');

function wordCount(text) {
  return text.trim().split(/\s+/).filter(Boolean).length;
}

/**
 * @param {string} subject
 * @param {string} body
 * @param {number} step  sequence step (0–4)
 * @returns {{ valid: boolean, reason?: string }}
 */
export function validate(subject, body, step) {
  // Rule 1: No HTML
  if (/<[a-z][\s\S]*>/i.test(body)) {
    return { valid: false, reason: 'HTML detected in body' };
  }

  // Rule 2: Word count
  const wc = wordCount(body);
  if (wc < MIN_WORDS || wc > MAX_WORDS) {
    return { valid: false, reason: `Word count ${wc} outside range ${MIN_WORDS}–${MAX_WORDS}` };
  }

  // Rule 3: No URL in step 0 or 1
  if (step <= 1 && /https?:\/\//i.test(body)) {
    return { valid: false, reason: 'URL found in step 0/1 body' };
  }

  // Rule 4: No spam words
  const spamWords = (process.env.SPAM_WORDS || '').split(',').map(w => w.trim().toLowerCase()).filter(Boolean);
  const bodyLower = body.toLowerCase();
  for (const word of spamWords) {
    if (bodyLower.includes(word)) {
      return { valid: false, reason: `Spam word detected: "${word}"` };
    }
  }

  // Rule 5: Subject ≤8 words, no ! or ? or ALL CAPS word
  const subjectWords = subject.trim().split(/\s+/);
  if (subjectWords.length > 8) {
    return { valid: false, reason: 'Subject exceeds 8 words' };
  }
  if (/[!?]/.test(subject)) {
    return { valid: false, reason: 'Subject contains ! or ?' };
  }
  if (/\b[A-Z]{3,}\b/.test(subject)) {
    return { valid: false, reason: 'Subject contains ALL CAPS word' };
  }

  // Rule 6: No unfilled template variables
  if (/\{\{/.test(body) || /\{\{/.test(subject)) {
    return { valid: false, reason: 'Unfilled template variable {{ }} detected' };
  }

  return { valid: true };
}
```

- [ ] **Step 4: Run test — expect PASS**

Run: `npx vitest run tests/utils/contentValidator.test.js`

- [ ] **Step 5: Commit**

```bash
git add utils/contentValidator.js tests/utils/contentValidator.test.js
git commit -m "feat: add utils/contentValidator.js — 6-rule pre-send check"
```

---

### Task 4.2: utils/blacklistCheck.js

- [ ] **Step 1: Write failing test** — `tests/utils/blacklistCheck.test.js`

```js
import { describe, it, expect, vi } from 'vitest';

vi.mock('dns', async () => {
  const actual = await vi.importActual('dns');
  return { ...actual, promises: { ...actual.promises, resolve: vi.fn() } };
});

describe('blacklistCheck', () => {
  it('returns clean=true when all DNS lookups fail (NXDOMAIN = not listed)', async () => {
    const dns = await import('dns');
    dns.promises.resolve.mockRejectedValue(new Error('ENOTFOUND'));
    const { checkDomain } = await import('../../utils/blacklistCheck.js');
    const result = await checkDomain('example.com');
    expect(result.clean).toBe(true);
    expect(result.zones).toEqual([]);
  });

  it('returns clean=false when DNS lookup resolves (listed)', async () => {
    const dns = await import('dns');
    dns.promises.resolve.mockResolvedValueOnce(['127.0.0.2']);
    dns.promises.resolve.mockRejectedValue(new Error('ENOTFOUND'));
    const { checkDomain } = await import('../../utils/blacklistCheck.js');
    const result = await checkDomain('spammy.com');
    expect(result.clean).toBe(false);
    expect(result.zones.length).toBeGreaterThan(0);
  });
});
```

- [ ] **Step 2: Run test — expect FAIL**

Run: `npx vitest run tests/utils/blacklistCheck.test.js`

- [ ] **Step 3: Implement utils/blacklistCheck.js**

```js
import dns from 'dns';

const BLACKLISTS = [
  'dbl.spamhaus.org',
  'b.barracudacentral.org',
  'multi.surbl.org'
];

/**
 * @param {string} domain  e.g. 'trysimpleinc.com'
 * @returns {Promise<{ clean: boolean, zones: string[] }>}
 */
export async function checkDomain(domain) {
  const listed = [];

  await Promise.all(BLACKLISTS.map(async (bl) => {
    const query = `${domain}.${bl}`;
    try {
      await dns.promises.resolve(query);
      listed.push(bl); // if resolves, domain IS listed
    } catch {
      // ENOTFOUND = not listed — expected and OK
    }
  }));

  return { clean: listed.length === 0, zones: listed };
}
```

- [ ] **Step 4: Run test — expect PASS**

Run: `npx vitest run tests/utils/blacklistCheck.test.js`

- [ ] **Step 5: Commit**

```bash
git add utils/blacklistCheck.js tests/utils/blacklistCheck.test.js
git commit -m "feat: add utils/blacklistCheck.js — DNS blacklist check"
```

---

### Task 4.3: healthCheck.js

- [ ] **Step 1: Implement healthCheck.js** (no dedicated test — uses integration of already-tested modules)

```js
import 'dotenv/config';
import { getDb, logCron, finishCron, logError, today } from './utils/db.js';
import { checkDomain } from './utils/blacklistCheck.js';
import { sendAlert } from './utils/telegram.js';

const DOMAIN = process.env.OUTREACH_DOMAIN || 'trysimpleinc.com';

export default async function healthCheck() {
  const cronId = logCron('healthCheck');
  try {
    // DNS blacklist check
    const { clean, zones } = await checkDomain(DOMAIN);
    if (!clean) {
      await sendAlert(`🚨 BLACKLIST: ${DOMAIN} listed on: ${zones.join(', ')} — sending paused`);
      // Set DAILY_SEND_LIMIT=0 by writing to process env (runtime override)
      process.env.DAILY_SEND_LIMIT = '0';
      logError('healthCheck.blacklist', new Error(`Domain ${DOMAIN} listed on ${zones.join(', ')}`));
    }

    // Bounce rate check (7-day rolling)
    const db = getDb();
    const rows = db.prepare(`
      SELECT SUM(emails_sent) as sent, SUM(bounces) as bounced
      FROM daily_metrics
      WHERE date >= date('now', '-7 days')
    `).get();
    const bounceRate = rows?.sent > 0 ? (rows.bounced / rows.sent) : 0;
    if (bounceRate > parseFloat(process.env.BOUNCE_RATE_HARD_STOP || '0.02')) {
      await sendAlert(`🚨 BOUNCE RATE ${(bounceRate * 100).toFixed(2)}% exceeds threshold — sending paused`);
      process.env.DAILY_SEND_LIMIT = '0';
    }

    // Unsub rate check (7-day rolling)
    const replyRows = db.prepare(`
      SELECT COUNT(*) as unsubs
      FROM replies
      WHERE classification='unsubscribe'
        AND received_at >= datetime('now', '-7 days')
    `).get();
    const unsubRate = rows?.sent > 0 ? ((replyRows?.unsubs || 0) / rows.sent) : 0;
    if (unsubRate > 0.01) {
      await sendAlert(`⚠️ UNSUB RATE ${(unsubRate * 100).toFixed(2)}% exceeds 1.0% — monitor closely`);
    }

    const summary = `✅ healthCheck: blacklist=${clean ? 'clean' : '🚨LISTED'}, bounce=${(bounceRate * 100).toFixed(2)}%, unsub=${(unsubRate * 100).toFixed(2)}%`;
    await sendAlert(summary);
    finishCron(cronId, { status: 'ok' });
  } catch (err) {
    logError('healthCheck', err);
    finishCron(cronId, { status: 'error', error: err.message });
    await sendAlert(`🚨 healthCheck error: ${err.message}`);
  }
}
```

- [ ] **Step 2: Commit**

```bash
git add healthCheck.js
git commit -m "feat: add healthCheck.js — Layer 4 health monitor"
```

---

### Task 4.4: Run all Chunk 4 tests

- [ ] **Step 1:** Run: `npx vitest run` — Expected: All tests PASS.

- [ ] **Step 2: Commit**

```bash
git commit -m "chore: chunk 4 anti-spam complete" --allow-empty
```

---

## Chunk 5: Email Infrastructure

**Files:**
- Create: `utils/mailer.js`
- Create: `utils/imap.js`
- Create: `tests/utils/mailer.test.js`

---

### Task 5.1: utils/mailer.js

- [ ] **Step 1: Write failing test** — `tests/utils/mailer.test.js`

```js
import { describe, it, expect, vi } from 'vitest';

vi.mock('nodemailer', () => ({
  default: {
    createTransport: vi.fn(() => ({
      verify: vi.fn(async () => true),
      sendMail: vi.fn(async (opts) => ({ messageId: '<test@test.com>' }))
    }))
  }
}));

describe('mailer', () => {
  it('sendMail returns messageId', async () => {
    process.env.INBOX_1_USER = 'darshan@trysimpleinc.com';
    process.env.INBOX_1_PASS = 'testpass';
    process.env.INBOX_2_USER = 'hello@trysimpleinc.com';
    process.env.INBOX_2_PASS = 'testpass2';
    process.env.SMTP_HOST = 'smtp.gmail.com';
    process.env.SMTP_PORT = '587';
    const { sendMail } = await import('../../utils/mailer.js');
    const result = await sendMail(1, {
      to: 'test@example.com',
      subject: 'Test subject',
      text: 'Test body',
      messageId: '<parent@test.com>'
    });
    expect(result.messageId).toBeTruthy();
  });

  it('verifyConnections resolves without throwing', async () => {
    const { verifyConnections } = await import('../../utils/mailer.js');
    await expect(verifyConnections()).resolves.not.toThrow();
  });
});
```

- [ ] **Step 2: Run test — expect FAIL**

Run: `npx vitest run tests/utils/mailer.test.js`

- [ ] **Step 3: Implement utils/mailer.js**

```js
import nodemailer from 'nodemailer';
import 'dotenv/config';

function buildTransport(user, pass) {
  return nodemailer.createTransport({
    host: process.env.SMTP_HOST || 'smtp.gmail.com',
    port: parseInt(process.env.SMTP_PORT || '587'),
    secure: false,
    auth: { user, pass }
  });
}

function getTransports() {
  return [
    buildTransport(process.env.INBOX_1_USER, process.env.INBOX_1_PASS),
    buildTransport(process.env.INBOX_2_USER, process.env.INBOX_2_PASS)
  ];
}

export async function verifyConnections() {
  const transports = getTransports();
  await Promise.all(transports.map(t => t.verify()));
}

/**
 * @param {1|2} inboxNumber
 * @param {{ to, subject, text, inReplyTo?: string, references?: string }} opts
 * @returns {Promise<{ messageId: string }>}
 */
export async function sendMail(inboxNumber, { to, subject, text, inReplyTo, references }) {
  const domain = process.env.OUTREACH_DOMAIN || 'trysimpleinc.com';
  const user = inboxNumber === 1 ? process.env.INBOX_1_USER : process.env.INBOX_2_USER;
  const pass = inboxNumber === 1 ? process.env.INBOX_1_PASS : process.env.INBOX_2_PASS;

  // Non-negotiable: assert outreach domain before every send
  if (!user?.endsWith(`@${domain}`)) {
    throw new Error(`Inbox ${inboxNumber} user ${user} is not on outreach domain ${domain}`);
  }

  const transport = buildTransport(user, pass);
  const mailOpts = {
    from: `Darshan Parmar <${user}>`,
    to,
    subject,
    text,
    ...(inReplyTo ? { inReplyTo, references: references || inReplyTo } : {})
  };

  const info = await transport.sendMail(mailOpts);
  return { messageId: info.messageId };
}
```

- [ ] **Step 4: Run test — expect PASS**

Run: `npx vitest run tests/utils/mailer.test.js`

- [ ] **Step 5: Commit**

```bash
git add utils/mailer.js tests/utils/mailer.test.js
git commit -m "feat: add utils/mailer.js — dual-inbox GWS SMTP"
```

---

### Task 5.2: utils/imap.js

- [ ] **Step 1: Implement utils/imap.js** (integration module — tested indirectly via checkReplies tests)

```js
import { ImapFlow } from 'imapflow';
import { simpleParser } from 'mailparser';
import 'dotenv/config';

function buildClient(user, pass) {
  return new ImapFlow({
    host: process.env.IMAP_HOST || 'imap.gmail.com',
    port: parseInt(process.env.IMAP_PORT || '993'),
    secure: true,
    auth: { user, pass },
    logger: false
  });
}

/**
 * Fetch all unseen messages from one inbox.
 * @param {1|2} inboxNumber
 * @returns {Promise<Array<{ uid, from, subject, text, date, messageId }>>}
 */
export async function fetchUnseen(inboxNumber) {
  const user = inboxNumber === 1 ? process.env.INBOX_1_USER : process.env.INBOX_2_USER;
  const pass = inboxNumber === 1 ? process.env.INBOX_1_PASS : process.env.INBOX_2_PASS;

  const client = buildClient(user, pass);
  const messages = [];

  try {
    await client.connect();
    await client.mailboxOpen('INBOX');

    for await (const msg of client.fetch({ seen: false }, { source: true })) {
      const parsed = await simpleParser(msg.source);
      messages.push({
        uid: msg.uid,
        from: parsed.from?.value?.[0]?.address || '',
        subject: parsed.subject || '',
        text: parsed.text || '',
        date: parsed.date,
        messageId: parsed.messageId || ''
      });
    }
  } finally {
    await client.logout();
  }

  return messages;
}
```

Note: `mailparser` is part of the `nodemailer` ecosystem. Add it to package.json:

```bash
npm install mailparser
```

- [ ] **Step 2: Commit**

```bash
git add utils/imap.js
git commit -m "feat: add utils/imap.js — dual-inbox IMAP reader"
```

---

### Task 5.3: Run all Chunk 5 tests

- [ ] **Step 1:** Run: `npx vitest run` — Expected: All tests PASS.

- [ ] **Step 2: Commit**

```bash
git commit -m "chore: chunk 5 email infrastructure complete" --allow-empty
```

---

## Chunk 6: Engines 3, 4, 5

**Files:**
- Create: `sendEmails.js`
- Create: `sendFollowups.js`
- Create: `checkReplies.js`
- Create: `dailyReport.js`
- Create: `backup.sh`
- Create: `tests/sendEmails.test.js`
- Create: `tests/checkReplies.test.js`

---

### Task 6.1: sendEmails.js

- [ ] **Step 1: Write failing test** — `tests/sendEmails.test.js`

```js
import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
import { mkdtempSync, rmSync } from 'fs';
import { tmpdir } from 'os';
import { join } from 'path';

vi.mock('../utils/mailer.js', () => ({
  verifyConnections: vi.fn(async () => {}),
  sendMail: vi.fn(async () => ({ messageId: '<abc@test.com>' }))
}));
vi.mock('../utils/telegram.js', () => ({ sendAlert: vi.fn(async () => {}) }));
vi.mock('../utils/contentValidator.js', () => ({ validate: vi.fn(() => ({ valid: true })) }));

let tmpDir;
beforeEach(async () => {
  tmpDir = mkdtempSync(join(tmpdir(), 'radar-test-'));
  process.env.DB_PATH = join(tmpDir, 'radar.sqlite');
  process.env.DAILY_SEND_LIMIT = '10';
  process.env.OUTREACH_DOMAIN = 'trysimpleinc.com';
  process.env.BOUNCE_RATE_HARD_STOP = '0.02';
  process.env.SEND_WINDOW_START_IST = '9';
  process.env.SEND_WINDOW_END_IST = '17';
  const { resetDb, initSchema, getDb } = await import('../utils/db.js');
  resetDb();
  initSchema();
  // Insert a ready lead
  getDb().prepare(`
    INSERT INTO leads (company, contact_email, icp_priority, email_subject, email_body, status)
    VALUES ('Acme', 'john@acme.com', 'A', 'Test subject', 'Test body with enough words to pass validation checks for the test', 'ready')
  `).run();
});

afterEach(async () => {
  const { resetDb } = await import('../utils/db.js');
  resetDb();
  rmSync(tmpDir, { recursive: true });
});

describe('sendEmails', () => {
  it('sends emails to ready leads and updates status', async () => {
    // Mock time inside send window
    vi.setSystemTime(new Date('2026-03-23T05:00:00Z')); // 10:30 AM IST
    const sendEmails = (await import('../sendEmails.js')).default;
    await sendEmails();
    const { getDb } = await import('../utils/db.js');
    const sent = getDb().prepare(`SELECT * FROM emails WHERE status='sent'`).all();
    expect(sent.length).toBeGreaterThan(0);
    vi.useRealTimers();
  });
});
```

- [ ] **Step 2: Run test — expect FAIL**

Run: `npx vitest run tests/sendEmails.test.js`

- [ ] **Step 3: Implement sendEmails.js**

```js
import 'dotenv/config';
import { getDb, logCron, finishCron, logError, bumpMetric, isRejected, todayBounceRate, todaySentCount } from './utils/db.js';
import { verifyConnections, sendMail } from './utils/mailer.js';
import { validate } from './utils/contentValidator.js';
import { sendAlert } from './utils/telegram.js';

// Indian holidays (hardcoded per spec)
const HOLIDAYS = ['01-26', '08-15', '10-02'];
// Diwali week 2026 approximation — add as needed
const HOLIDAY_RANGES = [];

function isHoliday(date) {
  const mmdd = date.toISOString().slice(5, 10);
  return HOLIDAYS.includes(mmdd);
}

function inSendWindow() {
  const now = new Date();
  // Convert to IST (UTC+5:30)
  const istOffset = 5.5 * 60 * 60 * 1000;
  const ist = new Date(now.getTime() + istOffset);
  const day = ist.getUTCDay(); // 0=Sun
  if (day === 0) return false; // No Sunday sending
  if (isHoliday(ist)) return false;
  const hour = ist.getUTCHours();
  const start = parseInt(process.env.SEND_WINDOW_START_IST || '9');
  const end = parseInt(process.env.SEND_WINDOW_END_IST || '17');
  return hour >= start && hour < end;
}

export default async function sendEmails() {
  const cronId = logCron('sendEmails');
  let sent = 0;
  let totalCost = 0;

  try {
    // Check send window
    if (!inSendWindow()) {
      finishCron(cronId, { status: 'skipped' });
      return;
    }

    // Check DAILY_SEND_LIMIT=0 hard stop
    const limit = parseInt(process.env.DAILY_SEND_LIMIT || '0');
    if (limit === 0) {
      finishCron(cronId, { status: 'skipped' });
      return;
    }

    // Check bounce rate
    const bounceRate = todayBounceRate();
    if (bounceRate > parseFloat(process.env.BOUNCE_RATE_HARD_STOP || '0.02')) {
      await sendAlert(`🚨 Bounce rate ${(bounceRate * 100).toFixed(2)}% — sending paused`);
      finishCron(cronId, { status: 'paused', error: 'bounce rate exceeded' });
      return;
    }

    // Already sent enough today?
    const alreadySent = todaySentCount();
    if (alreadySent >= limit) {
      finishCron(cronId, { status: 'limit_reached', emailsSent: alreadySent });
      return;
    }

    await verifyConnections();

    const db = getDb();
    const remaining = limit - alreadySent;
    const leads = db.prepare(`
      SELECT l.* FROM leads l
      WHERE l.status = 'ready'
        AND l.icp_priority IN ('A', 'B')
        AND l.contact_email NOT IN (SELECT email FROM reject_list)
      ORDER BY l.icp_priority ASC, l.icp_score DESC
      LIMIT ?
    `).all(remaining);

    let inboxCounter = alreadySent; // round-robin

    for (const lead of leads) {
      try {
        if (isRejected(lead.contact_email)) continue;

        // Content validation
        let validation = validate(lead.email_subject, lead.email_body, 0);
        if (!validation.valid) {
          // Regenerate once — for now log and skip (regeneration requires claude.js call)
          logError('sendEmails.contentValidation', new Error(`Lead ${lead.id}: ${validation.reason}`));
          db.prepare(`UPDATE leads SET status='content_rejected' WHERE id=?`).run(lead.id);
          continue;
        }

        const inboxNum = (inboxCounter % 2 === 0) ? 1 : 2;
        inboxCounter++;

        const { messageId } = await sendMail(inboxNum, {
          to: lead.contact_email,
          subject: lead.email_subject,
          text: lead.email_body
        });

        // Log email
        const emailId = db.prepare(`
          INSERT INTO emails (lead_id, sequence_step, inbox, subject, body, message_id, status, sent_at)
          VALUES (?, 0, ?, ?, ?, ?, 'sent', datetime('now'))
          RETURNING id
        `).get(lead.id, `inbox${inboxNum}`, lead.email_subject, lead.email_body, messageId).id;

        // Update lead status
        db.prepare(`UPDATE leads SET status='contacted', updated_at=datetime('now') WHERE id=?`).run(lead.id);

        // Init sequence state
        db.prepare(`
          INSERT OR REPLACE INTO sequence_state (lead_id, current_step, next_send_at, last_message_id, status)
          VALUES (?, 0, date('now', '+3 days'), ?, 'active')
        `).run(lead.id, messageId);

        bumpMetric('emails_sent');
        sent++;

        // Delay between sends (180–420s)
        const { sleep } = await import('./utils/sleep.js');
        await sleep(
          parseInt(process.env.SEND_DELAY_MIN_MS || '180000'),
          parseInt(process.env.SEND_DELAY_MAX_MS || '420000')
        );
      } catch (leadErr) {
        logError('sendEmails.lead', leadErr);
      }
    }

    finishCron(cronId, { status: 'ok', emailsSent: sent });
    await sendAlert(`✅ sendEmails: ${sent} sent today`);
  } catch (err) {
    logError('sendEmails', err);
    finishCron(cronId, { status: 'error', error: err.message });
    await sendAlert(`🚨 sendEmails failed: ${err.message}`);
  }
}
```

- [ ] **Step 4: Run test — expect PASS**

Run: `npx vitest run tests/sendEmails.test.js`

- [ ] **Step 5: Commit**

```bash
git add sendEmails.js tests/sendEmails.test.js
git commit -m "feat: add sendEmails.js — cold email send engine"
```

---

### Task 6.2: sendFollowups.js

- [ ] **Step 1: Implement sendFollowups.js**

```js
import 'dotenv/config';
import { getDb, logCron, finishCron, logError, bumpMetric } from './utils/db.js';
import { sendMail } from './utils/mailer.js';
import { validate } from './utils/contentValidator.js';
import { callClaude } from './utils/claude.js';
import { sendAlert } from './utils/telegram.js';
import { sleep } from './utils/sleep.js';

// Step config: offset days and whether it's a new thread
const STEPS = [
  { step: 1, offsetDays: 3,  newThread: false, angle: 'Short bump — just checking in' },
  { step: 2, offsetDays: 7,  newThread: false, angle: 'Value — brief case study or problem you solve' },
  { step: 3, offsetDays: 14, newThread: false, angle: 'Breakup — last email, no hard feelings' },
  { step: 4, offsetDays: 90, newThread: true,  angle: 'Quarterly nurture — fresh angle' }
];

async function generateFollowup(lead, angle, step) {
  const wordRange = step === 1 ? '20–35' : step === 3 ? '40–55' : '50–80';
  const result = await callClaude('haiku',
    `Write a follow-up email from Darshan Parmar to ${lead.contact_name || 'the owner'} at ${lead.company}.
Angle: ${angle}
Word count: ${wordRange} words
Plain text only, no links, CTA is to reply.
Return only the email body.`,
    { maxTokens: 150 }
  );
  return result.text.trim();
}

export default async function sendFollowups() {
  const cronId = logCron('sendFollowups');
  let sent = 0;

  try {
    const db = getDb();
    // Find sequence states due today
    const dueSequences = db.prepare(`
      SELECT ss.*, l.contact_email, l.contact_name, l.company, l.email_subject,
             l.icp_priority
      FROM sequence_state ss
      JOIN leads l ON ss.lead_id = l.id
      WHERE ss.status = 'active'
        AND ss.next_send_at <= date('now')
        AND ss.current_step < 4
        AND l.status NOT IN ('replied', 'unsubscribed')
    `).all();

    let inboxCounter = 0;
    for (const seq of dueSequences) {
      try {
        const nextStep = seq.current_step + 1;
        const stepConfig = STEPS.find(s => s.step === nextStep);
        if (!stepConfig) continue;

        const body = await generateFollowup(seq, stepConfig.angle, nextStep);
        const subject = nextStep === 4
          ? `Checking in again — ${seq.company}`
          : `Re: ${seq.email_subject}`;

        const validation = validate(subject, body, nextStep);
        if (!validation.valid) {
          logError('sendFollowups.validation', new Error(`Step ${nextStep} lead ${seq.lead_id}: ${validation.reason}`));
          continue;
        }

        const inboxNum = (inboxCounter % 2 === 0) ? 1 : 2;
        inboxCounter++;

        const mailOpts = {
          to: seq.contact_email,
          subject,
          text: body,
          ...(stepConfig.newThread ? {} : {
            inReplyTo: seq.last_message_id,
            references: [seq.last_references, seq.last_message_id].filter(Boolean).join(' ')
          })
        };

        const { messageId } = await sendMail(inboxNum, mailOpts);

        // Log email
        db.prepare(`
          INSERT INTO emails (lead_id, sequence_step, inbox, subject, body, message_id, status, sent_at)
          VALUES (?, ?, ?, ?, ?, ?, 'sent', datetime('now'))
        `).run(seq.lead_id, nextStep, `inbox${inboxNum}`, subject, body, messageId);

        // Compute next send date
        const nextStepConfig = STEPS.find(s => s.step === nextStep + 1);
        const nextSendAt = nextStepConfig
          ? `date('now', '+${nextStepConfig.offsetDays} days')`
          : null;

        // Update sequence state
        db.prepare(`
          UPDATE sequence_state
          SET current_step=?, next_send_at=${nextSendAt ? nextSendAt : 'NULL'},
              last_message_id=?,
              last_references=?,
              status=?,
              updated_at=datetime('now')
          WHERE id=?
        `).run(
          nextStep,
          messageId,
          [seq.last_references, seq.last_message_id].filter(Boolean).join(' '),
          nextStep >= 4 ? 'completed' : 'active',
          seq.id
        );

        bumpMetric('emails_sent');
        sent++;
        await sleep(180000, 420000);
      } catch (seqErr) {
        logError('sendFollowups.seq', seqErr);
      }
    }

    finishCron(cronId, { status: 'ok', emailsSent: sent });
    await sendAlert(`✅ sendFollowups: ${sent} follow-ups sent`);
  } catch (err) {
    logError('sendFollowups', err);
    finishCron(cronId, { status: 'error', error: err.message });
    await sendAlert(`🚨 sendFollowups failed: ${err.message}`);
  }
}
```

- [ ] **Step 2: Commit**

```bash
git add sendFollowups.js
git commit -m "feat: add sendFollowups.js — 5-step sequence engine"
```

---

### Task 6.3: checkReplies.js

- [ ] **Step 1: Write failing test** — `tests/checkReplies.test.js`

```js
import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
import { mkdtempSync, rmSync } from 'fs';
import { tmpdir } from 'os';
import { join } from 'path';

vi.mock('../utils/imap.js', () => ({
  fetchUnseen: vi.fn(async () => [{
    uid: 1,
    from: 'john@acme.com',
    subject: 'Re: your email',
    text: 'Sounds interesting, let me know your rate',
    date: new Date(),
    messageId: '<reply@test.com>'
  }])
}));
vi.mock('../utils/claude.js', () => ({
  callClaude: vi.fn(async () => ({ text: 'hot', costUsd: 0.001, inputTokens: 50, outputTokens: 5 }))
}));
vi.mock('../utils/telegram.js', () => ({ sendAlert: vi.fn(async () => {}) }));

let tmpDir;
beforeEach(async () => {
  tmpDir = mkdtempSync(join(tmpdir(), 'radar-test-'));
  process.env.DB_PATH = join(tmpDir, 'radar.sqlite');
  const { resetDb, initSchema, getDb } = await import('../utils/db.js');
  resetDb();
  initSchema();
  getDb().prepare(`
    INSERT INTO leads (company, contact_email, status) VALUES ('Acme', 'john@acme.com', 'contacted')
  `).run();
});

afterEach(async () => {
  const { resetDb } = await import('../utils/db.js');
  resetDb();
  rmSync(tmpDir, { recursive: true });
});

describe('checkReplies', () => {
  it('classifies hot reply and updates lead status', async () => {
    const checkReplies = (await import('../checkReplies.js')).default;
    await checkReplies();
    const { getDb } = await import('../utils/db.js');
    const lead = getDb().prepare(`SELECT status FROM leads WHERE contact_email='john@acme.com'`).get();
    expect(lead.status).toBe('replied');
    const reply = getDb().prepare(`SELECT * FROM replies`).get();
    expect(reply.classification).toBe('hot');
  });
});
```

- [ ] **Step 2: Run test — expect FAIL**

Run: `npx vitest run tests/checkReplies.test.js`

- [ ] **Step 3: Implement checkReplies.js**

```js
import 'dotenv/config';
import { getDb, logCron, finishCron, logError, bumpMetric, addToRejectList } from './utils/db.js';
import { fetchUnseen } from './utils/imap.js';
import { callClaude } from './utils/claude.js';
import { sendAlert } from './utils/telegram.js';

const CLASSIFY_PROMPT = (body) => `Classify this email reply into exactly one category. Return only the category word.

Categories:
- hot: interested, wants to know rate/price, "sounds good", ready to discuss
- schedule: wants to book a call/meeting/zoom
- soft_no: maybe later, not right now, send portfolio first
- unsubscribe: not interested, remove me, stop emailing
- ooo: out of office, on leave, automatic reply
- other: anything else

Reply text:
"""
${body.slice(0, 500)}
"""

Return only one word: hot | schedule | soft_no | unsubscribe | ooo | other`;

export default async function checkReplies() {
  const cronId = logCron('checkReplies');
  let processed = 0;

  try {
    const db = getDb();

    for (const inboxNum of [1, 2]) {
      let messages;
      try {
        messages = await fetchUnseen(inboxNum);
      } catch (imapErr) {
        logError(`checkReplies.imap${inboxNum}`, imapErr);
        continue;
      }

      for (const msg of messages) {
        try {
          // Match to lead
          const lead = db.prepare(`SELECT * FROM leads WHERE contact_email=?`).get(msg.from);
          if (!lead) continue;

          // Find the most recent sent email for this lead
          const sentEmail = db.prepare(`
            SELECT * FROM emails WHERE lead_id=? AND status='sent' ORDER BY sent_at DESC LIMIT 1
          `).get(lead.id);

          // Classify with Haiku
          const { text: classification, costUsd } = await callClaude('haiku', CLASSIFY_PROMPT(msg.text), { maxTokens: 10 });
          const cat = classification.trim().toLowerCase();

          // Insert reply
          db.prepare(`
            INSERT INTO replies (lead_id, email_id, inbox, subject, body, classification, classify_cost_usd)
            VALUES (?, ?, ?, ?, ?, ?, ?)
          `).run(lead.id, sentEmail?.id || null, `inbox${inboxNum}`, msg.subject, msg.text, cat, costUsd);

          bumpMetric('replies');
          bumpMetric('haiku_cost_usd', costUsd);

          // Act on classification
          switch (cat) {
            case 'hot':
              db.prepare(`UPDATE leads SET status='replied', updated_at=datetime('now') WHERE id=?`).run(lead.id);
              db.prepare(`UPDATE sequence_state SET status='replied' WHERE lead_id=?`).run(lead.id);
              bumpMetric('hot_replies');
              await sendAlert(`🔥 HOT reply from ${lead.company} (${msg.from}): "${msg.text.slice(0, 100)}"`);
              break;
            case 'schedule':
              db.prepare(`UPDATE leads SET status='replied', updated_at=datetime('now') WHERE id=?`).run(lead.id);
              db.prepare(`UPDATE sequence_state SET status='replied' WHERE lead_id=?`).run(lead.id);
              await sendAlert(`📅 SCHEDULE reply from ${lead.company} (${msg.from})`);
              break;
            case 'soft_no':
              db.prepare(`UPDATE sequence_state SET next_send_at=date('now', '+14 days'), status='paused' WHERE lead_id=?`).run(lead.id);
              break;
            case 'unsubscribe':
              addToRejectList(msg.from, 'unsubscribe');
              db.prepare(`UPDATE leads SET status='unsubscribed' WHERE id=?`).run(lead.id);
              db.prepare(`UPDATE sequence_state SET status='stopped' WHERE lead_id=?`).run(lead.id);
              break;
            case 'ooo':
              db.prepare(`UPDATE sequence_state SET next_send_at=date('now', '+5 days') WHERE lead_id=?`).run(lead.id);
              break;
          }

          processed++;
        } catch (msgErr) {
          logError('checkReplies.msg', msgErr);
        }
      }
    }

    finishCron(cronId, { status: 'ok' });
  } catch (err) {
    logError('checkReplies', err);
    finishCron(cronId, { status: 'error', error: err.message });
    await sendAlert(`🚨 checkReplies failed: ${err.message}`);
  }
}
```

- [ ] **Step 4: Run test — expect PASS**

Run: `npx vitest run tests/checkReplies.test.js`

- [ ] **Step 5: Commit**

```bash
git add checkReplies.js tests/checkReplies.test.js
git commit -m "feat: add checkReplies.js — reply classification engine"
```

---

### Task 6.4: dailyReport.js + backup.sh

- [ ] **Step 1: Implement dailyReport.js**

```js
import 'dotenv/config';
import { getDb, logCron, finishCron, logError, today } from './utils/db.js';
import { callClaude } from './utils/claude.js';
import { sendMail } from './utils/mailer.js';
import { sendAlert } from './utils/telegram.js';

export default async function dailyReport() {
  const cronId = logCron('dailyReport');
  try {
    const db = getDb();
    const d = today();

    // Aggregate today's metrics
    const metrics = db.prepare(`SELECT * FROM daily_metrics WHERE date=?`).get(d) || {};
    const errors = db.prepare(`SELECT COUNT(*) as count FROM error_log WHERE date(created_at)=? AND resolved=0`).get(d);
    const hotLeads = db.prepare(`SELECT company, contact_email FROM leads WHERE status='replied' AND date(updated_at)=?`).all(d);

    // Telegram one-liner
    const tgMsg = [
      `📊 Daily Report ${d}`,
      `Leads: ${metrics.leads_found || 0} | Sent: ${metrics.emails_sent || 0} | Replies: ${metrics.replies || 0} | Hot: ${metrics.hot_replies || 0}`,
      `Cost: $${(metrics.total_cost_usd || 0).toFixed(4)} | Errors: ${errors?.count || 0}`
    ].join('\n');
    await sendAlert(tgMsg);

    // Generate HTML report via Haiku
    const reportPrompt = `Generate a clean HTML email report for a cold email outreach campaign.

Stats for ${d}:
- Leads found: ${metrics.leads_found || 0}
- Emails sent: ${metrics.emails_sent || 0}
- Replies: ${metrics.replies || 0}
- Hot replies: ${metrics.hot_replies || 0}
- Bounces: ${metrics.bounces || 0}
- Total AI cost: $${(metrics.total_cost_usd || 0).toFixed(4)}
- Unresolved errors: ${errors?.count || 0}
- Hot leads today: ${hotLeads.map(l => l.company).join(', ') || 'none'}

Return a complete HTML email with inline styles. Professional, clean design. No external CSS.`;

    const { text: htmlBody, costUsd } = await callClaude('haiku', reportPrompt, { maxTokens: 1500 });

    // Send HTML report email to owner
    await sendMail(1, {
      to: process.env.REPORT_EMAIL || 'darshan@simpleinc.in',
      subject: `Radar Daily Report — ${d}`,
      text: `Leads: ${metrics.leads_found || 0}, Sent: ${metrics.emails_sent || 0}, Replies: ${metrics.replies || 0}, Hot: ${metrics.hot_replies || 0}`
    });

    // Store aggregated metrics (no html_body column per spec)
    db.prepare(`
      INSERT INTO daily_metrics (date, leads_found, emails_sent, bounces, replies, hot_replies,
        gemini_cost_usd, sonnet_cost_usd, haiku_cost_usd, total_cost_usd)
      VALUES (?,?,?,?,?,?,?,?,?,?)
      ON CONFLICT(date) DO UPDATE SET
        leads_found=excluded.leads_found, emails_sent=excluded.emails_sent,
        bounces=excluded.bounces, replies=excluded.replies, hot_replies=excluded.hot_replies,
        gemini_cost_usd=excluded.gemini_cost_usd, sonnet_cost_usd=excluded.sonnet_cost_usd,
        haiku_cost_usd=excluded.haiku_cost_usd, total_cost_usd=excluded.total_cost_usd
    `).run(d,
      metrics.leads_found || 0, metrics.emails_sent || 0,
      metrics.bounces || 0, metrics.replies || 0, metrics.hot_replies || 0,
      metrics.gemini_cost_usd || 0, metrics.sonnet_cost_usd || 0,
      (metrics.haiku_cost_usd || 0) + costUsd,
      (metrics.total_cost_usd || 0) + costUsd
    );

    finishCron(cronId, { status: 'ok' });
  } catch (err) {
    logError('dailyReport', err);
    finishCron(cronId, { status: 'error', error: err.message });
    await sendAlert(`🚨 dailyReport failed: ${err.message}`);
  }
}
```

- [ ] **Step 2: Create backup.sh**

```bash
#!/usr/bin/env bash
set -euo pipefail

DB_PATH="${DB_PATH:-/home/radar/db/radar.sqlite}"
BACKUP_DIR="${BACKUP_DIR:-/home/radar/backups}"
DATE=$(date +%Y-%m-%d)

mkdir -p "$BACKUP_DIR"
sqlite3 "$DB_PATH" ".backup '$BACKUP_DIR/radar-$DATE.sqlite'"

# Upload to Backblaze B2 via rclone (credentials configured separately)
if command -v rclone &> /dev/null; then
  rclone copy "$BACKUP_DIR/radar-$DATE.sqlite" b2:radar-backups/
fi

# Keep only last 30 days
find "$BACKUP_DIR" -name "radar-*.sqlite" -mtime +30 -delete

echo "Backup complete: radar-$DATE.sqlite"
```

```bash
chmod +x backup.sh
```

- [ ] **Step 3: Commit**

```bash
git add dailyReport.js backup.sh
git commit -m "feat: add dailyReport.js and backup.sh"
```

---

### Task 6.5: Run all Chunk 6 tests

- [ ] **Step 1:** Run: `npx vitest run` — Expected: All tests PASS.

- [ ] **Step 2: Commit**

```bash
git commit -m "chore: chunk 6 engines 3-5 complete" --allow-empty
```

---

## Chunk 7: Dashboard API

**Files:**
- Create: `dashboard/package.json`
- Create: `dashboard/server.js`
- Create: `tests/dashboard/api.test.js`

---

### Task 7.1: dashboard/package.json

- [ ] **Step 1: Create dashboard/package.json**

```json
{
  "name": "radar-dashboard",
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.0",
    "react-dom": "^18.3.0",
    "react-router-dom": "^6.23.0",
    "recharts": "^2.12.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.0",
    "vite": "^5.3.0"
  }
}
```

- [ ] **Step 2: Install**

Run: `cd dashboard && npm install && cd ..`

- [ ] **Step 3: Commit**

```bash
git add dashboard/package.json dashboard/package-lock.json
git commit -m "chore: add dashboard/package.json"
```

---

### Task 7.2: dashboard/server.js

- [ ] **Step 1: Write failing test** — `tests/dashboard/api.test.js`

```js
import { describe, it, expect, vi, beforeAll, afterAll } from 'vitest';
import { mkdtempSync, rmSync } from 'fs';
import { tmpdir } from 'os';
import { join } from 'path';

let app, tmpDir, server;

beforeAll(async () => {
  tmpDir = mkdtempSync(join(tmpdir(), 'radar-test-'));
  process.env.DB_PATH = join(tmpDir, 'radar.sqlite');
  process.env.DASHBOARD_PASSWORD = 'testpass';
  process.env.JWT_SECRET = 'testsecret64charslongpadded00000000000000000000000000000000000000';
  process.env.JWT_EXPIRES_IN = '7d';
  const { resetDb, initSchema } = await import('../../utils/db.js');
  resetDb();
  initSchema();
  const mod = await import('../../dashboard/server.js');
  app = mod.app;
});

afterAll(async () => {
  const { resetDb } = await import('../../utils/db.js');
  resetDb();
  rmSync(tmpDir, { recursive: true });
});

describe('dashboard API', () => {
  it('POST /api/auth/login returns token with correct password', async () => {
    const res = await fetch(`http://localhost:3001/api/auth/login`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ password: 'testpass' })
    });
    expect(res.status).toBe(200);
    const body = await res.json();
    expect(body.token).toBeTruthy();
  });

  it('GET /api/overview requires auth', async () => {
    const res = await fetch(`http://localhost:3001/api/overview`);
    expect(res.status).toBe(401);
  });
});
```

- [ ] **Step 2: Run test — expect FAIL**

Run: `npx vitest run tests/dashboard/api.test.js`

- [ ] **Step 3: Implement dashboard/server.js**

```js
import express from 'express';
import jwt from 'jsonwebtoken';
import bcrypt from 'bcrypt';
import 'dotenv/config';
import { getDb, today, logError } from '../utils/db.js';

export const app = express();
app.use(express.json());

// ── Auth ──────────────────────────────────────────────────────
const hashedPassword = bcrypt.hashSync(process.env.DASHBOARD_PASSWORD || 'changeme', 10);

app.post('/api/auth/login', (req, res) => {
  const { password } = req.body;
  if (!password || !bcrypt.compareSync(password, hashedPassword)) {
    return res.status(401).json({ error: 'Invalid password' });
  }
  const token = jwt.sign({}, process.env.JWT_SECRET, { expiresIn: process.env.JWT_EXPIRES_IN || '7d' });
  res.json({ token });
});

function requireAuth(req, res, next) {
  const auth = req.headers.authorization;
  if (!auth?.startsWith('Bearer ')) return res.status(401).json({ error: 'Unauthorized' });
  try {
    jwt.verify(auth.slice(7), process.env.JWT_SECRET);
    next();
  } catch {
    res.status(401).json({ error: 'Invalid token' });
  }
}

// ── Routes ────────────────────────────────────────────────────
app.get('/api/overview', requireAuth, (req, res) => {
  try {
    const db = getDb();
    const d = today();
    const metrics = db.prepare(`SELECT * FROM daily_metrics WHERE date=?`).get(d) || {};
    const hotLeads = db.prepare(`SELECT id, company, contact_email FROM leads WHERE status='replied' ORDER BY updated_at DESC LIMIT 10`).all();
    const chart90 = db.prepare(`SELECT date, emails_sent, replies, hot_replies FROM daily_metrics ORDER BY date DESC LIMIT 90`).all();
    res.json({ metrics, hotLeads, chart90 });
  } catch (err) { logError('api.overview', err); res.status(500).json({ error: err.message }); }
});

app.get('/api/leads', requireAuth, (req, res) => {
  try {
    const db = getDb();
    const page = parseInt(req.query.page || '1');
    const limit = parseInt(req.query.limit || '50');
    const offset = (page - 1) * limit;
    const status = req.query.status;
    const where = status ? `WHERE status=?` : '';
    const params = status ? [status, limit, offset] : [limit, offset];
    const leads = db.prepare(`SELECT * FROM leads ${where} ORDER BY created_at DESC LIMIT ? OFFSET ?`).all(...params);
    const total = db.prepare(`SELECT COUNT(*) as count FROM leads ${where}`).get(...(status ? [status] : []));
    res.json({ leads, total: total.count, page, limit });
  } catch (err) { logError('api.leads', err); res.status(500).json({ error: err.message }); }
});

app.get('/api/leads/:id', requireAuth, (req, res) => {
  try {
    const db = getDb();
    const lead = db.prepare(`SELECT * FROM leads WHERE id=?`).get(req.params.id);
    if (!lead) return res.status(404).json({ error: 'Not found' });
    const emails = db.prepare(`SELECT * FROM emails WHERE lead_id=? ORDER BY sent_at`).all(lead.id);
    const replies = db.prepare(`SELECT * FROM replies WHERE lead_id=? ORDER BY received_at`).all(lead.id);
    const seqState = db.prepare(`SELECT * FROM sequence_state WHERE lead_id=?`).get(lead.id);
    res.json({ lead, emails, replies, seqState });
  } catch (err) { logError('api.leads.id', err); res.status(500).json({ error: err.message }); }
});

app.patch('/api/leads/:id/status', requireAuth, (req, res) => {
  try {
    const { status } = req.body;
    if (!status) return res.status(400).json({ error: 'status required' });
    getDb().prepare(`UPDATE leads SET status=?, updated_at=datetime('now') WHERE id=?`).run(status, req.params.id);
    res.json({ ok: true });
  } catch (err) { logError('api.leads.status', err); res.status(500).json({ error: err.message }); }
});

app.get('/api/send-log', requireAuth, (req, res) => {
  try {
    const db = getDb();
    const page = parseInt(req.query.page || '1');
    const limit = parseInt(req.query.limit || '50');
    const offset = (page - 1) * limit;
    const emails = db.prepare(`SELECT e.*, l.company, l.contact_email FROM emails e JOIN leads l ON e.lead_id=l.id ORDER BY e.sent_at DESC LIMIT ? OFFSET ?`).all(limit, offset);
    const total = db.prepare(`SELECT COUNT(*) as count FROM emails`).get();
    res.json({ emails, total: total.count });
  } catch (err) { logError('api.sendLog', err); res.status(500).json({ error: err.message }); }
});

app.get('/api/replies', requireAuth, (req, res) => {
  try {
    const db = getDb();
    const replies = db.prepare(`
      SELECT r.*, l.company FROM replies r JOIN leads l ON r.lead_id=l.id
      ORDER BY CASE r.classification WHEN 'hot' THEN 0 WHEN 'schedule' THEN 1 ELSE 2 END, r.received_at DESC
    `).all();
    res.json({ replies });
  } catch (err) { logError('api.replies', err); res.status(500).json({ error: err.message }); }
});

app.get('/api/sequences', requireAuth, (req, res) => {
  try {
    const sequences = getDb().prepare(`
      SELECT ss.*, l.company, l.contact_email, l.icp_priority
      FROM sequence_state ss JOIN leads l ON ss.lead_id=l.id
      ORDER BY ss.next_send_at ASC
    `).all();
    res.json({ sequences });
  } catch (err) { logError('api.sequences', err); res.status(500).json({ error: err.message }); }
});

app.get('/api/cron-status', requireAuth, (req, res) => {
  try {
    const db = getDb();
    const jobs = db.prepare(`
      SELECT job_name, MAX(started_at) as last_run, status, error
      FROM cron_log WHERE date(started_at)=date('now')
      GROUP BY job_name
    `).all();
    res.json({ jobs });
  } catch (err) { logError('api.cronStatus', err); res.status(500).json({ error: err.message }); }
});

app.get('/api/cron-status/:job/history', requireAuth, (req, res) => {
  try {
    const history = getDb().prepare(`SELECT * FROM cron_log WHERE job_name=? ORDER BY started_at DESC LIMIT 30`).all(req.params.job);
    res.json({ history });
  } catch (err) { logError('api.cronHistory', err); res.status(500).json({ error: err.message }); }
});

app.get('/api/health', requireAuth, async (req, res) => {
  try {
    const db = getDb();
    const metrics7d = db.prepare(`
      SELECT SUM(emails_sent) as sent, SUM(bounces) as bounced FROM daily_metrics WHERE date>=date('now','-7 days')
    `).get();
    const bounceRate = metrics7d?.sent > 0 ? metrics7d.bounced / metrics7d.sent : 0;
    res.json({ bounceRate, domain: process.env.OUTREACH_DOMAIN });
  } catch (err) { logError('api.health', err); res.status(500).json({ error: err.message }); }
});

app.get('/api/costs', requireAuth, (req, res) => {
  try {
    const db = getDb();
    const daily30 = db.prepare(`SELECT * FROM daily_metrics ORDER BY date DESC LIMIT 30`).all();
    const totals = db.prepare(`
      SELECT SUM(gemini_cost_usd) as gemini, SUM(sonnet_cost_usd) as sonnet,
             SUM(haiku_cost_usd) as haiku, SUM(total_cost_usd) as total
      FROM daily_metrics WHERE date>=date('now','-30 days')
    `).get();
    res.json({ daily30, totals });
  } catch (err) { logError('api.costs', err); res.status(500).json({ error: err.message }); }
});

app.get('/api/errors', requireAuth, (req, res) => {
  try {
    const errors = getDb().prepare(`SELECT * FROM error_log ORDER BY created_at DESC LIMIT 200`).all();
    res.json({ errors });
  } catch (err) { logError('api.errors', err); res.status(500).json({ error: err.message }); }
});

app.patch('/api/errors/:id/resolve', requireAuth, (req, res) => {
  try {
    getDb().prepare(`UPDATE error_log SET resolved=1 WHERE id=?`).run(req.params.id);
    res.json({ ok: true });
  } catch (err) { logError('api.errors.resolve', err); res.status(500).json({ error: err.message }); }
});

// Serve React SPA (built files) for everything else
import { join, dirname } from 'path';
import { fileURLToPath } from 'url';
import { existsSync } from 'fs';
const __dirname = dirname(fileURLToPath(import.meta.url));
const distPath = join(__dirname, 'dist');
if (existsSync(distPath)) {
  app.use(express.static(distPath));
  app.get('*', (req, res) => res.sendFile(join(distPath, 'index.html')));
}

const PORT = parseInt(process.env.DASHBOARD_PORT || '3001');
if (process.env.NODE_ENV !== 'test') {
  app.listen(PORT, () => console.log(`Radar dashboard on port ${PORT}`));
}
```

- [ ] **Step 4: Run test — expect PASS**

Run: `npx vitest run tests/dashboard/api.test.js`

- [ ] **Step 5: Commit**

```bash
git add dashboard/server.js tests/dashboard/api.test.js
git commit -m "feat: add dashboard/server.js — Express API with JWT auth"
```

---

### Task 7.3: Run all Chunk 7 tests

- [ ] **Step 1:** Run: `npx vitest run` — Expected: All tests PASS.

- [ ] **Step 2: Commit**

```bash
git commit -m "chore: chunk 7 dashboard API complete" --allow-empty
```

---

## Chunk 8: Dashboard React (9 pages)

**Files:**
- Create: `dashboard/index.html`
- Create: `dashboard/vite.config.js`
- Create: `dashboard/src/main.jsx`
- Create: `dashboard/src/App.jsx`
- Create: `dashboard/src/api.js`
- Create: `dashboard/src/pages/Login.jsx`
- Create: `dashboard/src/pages/Overview.jsx`
- Create: `dashboard/src/pages/LeadPipeline.jsx`
- Create: `dashboard/src/pages/SendLog.jsx`
- Create: `dashboard/src/pages/ReplyFeed.jsx`
- Create: `dashboard/src/pages/SequenceTracker.jsx`
- Create: `dashboard/src/pages/CronStatus.jsx`
- Create: `dashboard/src/pages/HealthMonitor.jsx`
- Create: `dashboard/src/pages/CostTracker.jsx`
- Create: `dashboard/src/pages/ErrorLog.jsx`
- Create: `dashboard/src/components/Sidebar.jsx`
- Create: `dashboard/src/components/StatCard.jsx`

---

### Task 8.1: Vite setup

- [ ] **Step 1: Create dashboard/index.html**

```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Radar Dashboard</title>
    <link href="https://fonts.googleapis.com/css2?family=IBM+Plex+Mono:wght@400;500;600&display=swap" rel="stylesheet">
    <style>
      * { box-sizing: border-box; margin: 0; padding: 0; }
      body { font-family: 'IBM Plex Mono', monospace; background: #0f0f0f; color: #e0e0e0; }
    </style>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.jsx"></script>
  </body>
</html>
```

- [ ] **Step 2: Create dashboard/vite.config.js**

```js
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  server: { proxy: { '/api': 'http://localhost:3001' } }
});
```

- [ ] **Step 3: Commit**

```bash
git add dashboard/index.html dashboard/vite.config.js
git commit -m "chore: add Vite config and index.html"
```

---

### Task 8.2: api.js + App.jsx + main.jsx

- [ ] **Step 1: Create dashboard/src/api.js**

```js
const BASE = '/api';

function getToken() {
  return localStorage.getItem('radar_token');
}

async function request(path, opts = {}) {
  const res = await fetch(`${BASE}${path}`, {
    ...opts,
    headers: {
      'Content-Type': 'application/json',
      Authorization: `Bearer ${getToken()}`,
      ...(opts.headers || {})
    }
  });
  if (res.status === 401) {
    localStorage.removeItem('radar_token');
    window.location.href = '/login';
    return;
  }
  return res.json();
}

export const api = {
  login: (password) =>
    fetch(`${BASE}/auth/login`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ password }) }).then(r => r.json()),
  overview:      () => request('/overview'),
  leads:         (params = '') => request(`/leads${params}`),
  lead:          (id) => request(`/leads/${id}`),
  updateStatus:  (id, status) => request(`/leads/${id}/status`, { method: 'PATCH', body: JSON.stringify({ status }) }),
  sendLog:       (params = '') => request(`/send-log${params}`),
  replies:       () => request('/replies'),
  sequences:     () => request('/sequences'),
  cronStatus:    () => request('/cron-status'),
  cronHistory:   (job) => request(`/cron-status/${job}/history`),
  health:        () => request('/health'),
  costs:         () => request('/costs'),
  errors:        () => request('/errors'),
  resolveError:  (id) => request(`/errors/${id}/resolve`, { method: 'PATCH' })
};
```

- [ ] **Step 2: Create dashboard/src/main.jsx**

```jsx
import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

ReactDOM.createRoot(document.getElementById('root')).render(<App />);
```

- [ ] **Step 3: Create dashboard/src/App.jsx**

```jsx
import React, { useState, useEffect } from 'react';
import { BrowserRouter, Routes, Route, Navigate } from 'react-router-dom';
import Sidebar from './components/Sidebar';
import Login from './pages/Login';
import Overview from './pages/Overview';
import LeadPipeline from './pages/LeadPipeline';
import SendLog from './pages/SendLog';
import ReplyFeed from './pages/ReplyFeed';
import SequenceTracker from './pages/SequenceTracker';
import CronStatus from './pages/CronStatus';
import HealthMonitor from './pages/HealthMonitor';
import CostTracker from './pages/CostTracker';
import ErrorLog from './pages/ErrorLog';

function ProtectedLayout({ children }) {
  const token = localStorage.getItem('radar_token');
  if (!token) return <Navigate to="/login" replace />;
  return (
    <div style={{ display: 'flex', minHeight: '100vh' }}>
      <Sidebar />
      <main style={{ flex: 1, padding: '24px', overflowY: 'auto' }}>{children}</main>
    </div>
  );
}

export default function App() {
  return (
    <BrowserRouter>
      <Routes>
        <Route path="/login" element={<Login />} />
        <Route path="/" element={<ProtectedLayout><Overview /></ProtectedLayout>} />
        <Route path="/leads" element={<ProtectedLayout><LeadPipeline /></ProtectedLayout>} />
        <Route path="/send-log" element={<ProtectedLayout><SendLog /></ProtectedLayout>} />
        <Route path="/replies" element={<ProtectedLayout><ReplyFeed /></ProtectedLayout>} />
        <Route path="/sequences" element={<ProtectedLayout><SequenceTracker /></ProtectedLayout>} />
        <Route path="/cron" element={<ProtectedLayout><CronStatus /></ProtectedLayout>} />
        <Route path="/health" element={<ProtectedLayout><HealthMonitor /></ProtectedLayout>} />
        <Route path="/costs" element={<ProtectedLayout><CostTracker /></ProtectedLayout>} />
        <Route path="/errors" element={<ProtectedLayout><ErrorLog /></ProtectedLayout>} />
        <Route path="*" element={<Navigate to="/" replace />} />
      </Routes>
    </BrowserRouter>
  );
}
```

- [ ] **Step 4: Commit**

```bash
git add dashboard/src/
git commit -m "feat: add dashboard app shell (api.js, App.jsx, main.jsx)"
```

---

### Task 8.3: Sidebar + StatCard components

- [ ] **Step 1: Create dashboard/src/components/Sidebar.jsx**

```jsx
import React from 'react';
import { NavLink } from 'react-router-dom';

const NAV = [
  { to: '/',          label: 'Overview' },
  { to: '/leads',     label: 'Lead Pipeline' },
  { to: '/send-log',  label: 'Send Log' },
  { to: '/replies',   label: 'Reply Feed' },
  { to: '/sequences', label: 'Sequences' },
  { to: '/cron',      label: 'Cron Status' },
  { to: '/health',    label: 'Health Monitor' },
  { to: '/costs',     label: 'Cost Tracker' },
  { to: '/errors',    label: 'Error Log' }
];

export default function Sidebar() {
  const style = {
    sidebar: { width: 200, background: '#1a1a1a', padding: '24px 16px', borderRight: '1px solid #333', display: 'flex', flexDirection: 'column', gap: 8 },
    title: { fontSize: 14, fontWeight: 600, color: '#888', marginBottom: 16, letterSpacing: '0.1em' },
    link: { display: 'block', padding: '8px 12px', borderRadius: 6, color: '#aaa', textDecoration: 'none', fontSize: 13 },
    active: { background: '#2a2a2a', color: '#fff' }
  };
  return (
    <nav style={style.sidebar}>
      <div style={style.title}>RADAR v2</div>
      {NAV.map(n => (
        <NavLink key={n.to} to={n.to} end={n.to === '/'}
          style={({ isActive }) => ({ ...style.link, ...(isActive ? style.active : {}) })}>
          {n.label}
        </NavLink>
      ))}
      <div style={{ marginTop: 'auto' }}>
        <button onClick={() => { localStorage.removeItem('radar_token'); window.location.href = '/login'; }}
          style={{ ...style.link, background: 'none', border: 'none', cursor: 'pointer', color: '#666', width: '100%', textAlign: 'left' }}>
          Log out
        </button>
      </div>
    </nav>
  );
}
```

- [ ] **Step 2: Create dashboard/src/components/StatCard.jsx**

```jsx
import React from 'react';

export default function StatCard({ label, value, sub, color = '#4ade80' }) {
  return (
    <div style={{ background: '#1a1a1a', border: '1px solid #2a2a2a', borderRadius: 8, padding: '20px 24px', minWidth: 160 }}>
      <div style={{ fontSize: 12, color: '#666', marginBottom: 8, letterSpacing: '0.05em' }}>{label}</div>
      <div style={{ fontSize: 28, fontWeight: 600, color }}>{value ?? '—'}</div>
      {sub && <div style={{ fontSize: 11, color: '#555', marginTop: 6 }}>{sub}</div>}
    </div>
  );
}
```

- [ ] **Step 3: Commit**

```bash
git add dashboard/src/components/
git commit -m "feat: add Sidebar and StatCard components"
```

---

### Task 8.4: All 9 dashboard pages

- [ ] **Step 1: Create all 9 page files**

**dashboard/src/pages/Login.jsx**
```jsx
import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { api } from '../api';

export default function Login() {
  const [pw, setPw] = useState('');
  const [err, setErr] = useState('');
  const nav = useNavigate();

  async function handleSubmit(e) {
    e.preventDefault();
    const res = await api.login(pw);
    if (res.token) { localStorage.setItem('radar_token', res.token); nav('/'); }
    else setErr('Invalid password');
  }

  return (
    <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'center', minHeight: '100vh' }}>
      <form onSubmit={handleSubmit} style={{ background: '#1a1a1a', padding: 40, borderRadius: 12, width: 320 }}>
        <h2 style={{ marginBottom: 24, fontSize: 18 }}>Radar v2</h2>
        <input type="password" value={pw} onChange={e => setPw(e.target.value)}
          placeholder="Password" autoFocus
          style={{ width: '100%', padding: '10px 14px', background: '#0f0f0f', border: '1px solid #333', borderRadius: 6, color: '#e0e0e0', fontSize: 14, marginBottom: 16 }} />
        {err && <div style={{ color: '#f87171', fontSize: 12, marginBottom: 12 }}>{err}</div>}
        <button type="submit" style={{ width: '100%', padding: '10px', background: '#4ade80', border: 'none', borderRadius: 6, color: '#000', fontWeight: 600, cursor: 'pointer' }}>
          Login
        </button>
      </form>
    </div>
  );
}
```

**dashboard/src/pages/Overview.jsx**
```jsx
import React, { useEffect, useState } from 'react';
import { LineChart, Line, XAxis, YAxis, Tooltip, ResponsiveContainer } from 'recharts';
import StatCard from '../components/StatCard';
import { api } from '../api';

export default function Overview() {
  const [data, setData] = useState(null);
  useEffect(() => { api.overview().then(setData); }, []);
  if (!data) return <div>Loading...</div>;
  const { metrics, hotLeads, chart90 } = data;
  return (
    <div>
      <h1 style={{ marginBottom: 24, fontSize: 20 }}>Overview</h1>
      <div style={{ display: 'flex', gap: 16, flexWrap: 'wrap', marginBottom: 32 }}>
        <StatCard label="LEADS TODAY" value={metrics.leads_found} />
        <StatCard label="EMAILS SENT" value={metrics.emails_sent} />
        <StatCard label="REPLIES" value={metrics.replies} />
        <StatCard label="HOT" value={metrics.hot_replies} color="#f87171" />
        <StatCard label="COST TODAY" value={`$${(metrics.total_cost_usd || 0).toFixed(4)}`} color="#facc15" />
      </div>
      {hotLeads.length > 0 && (
        <div style={{ background: '#1a1a1a', border: '1px solid #ef4444', borderRadius: 8, padding: 20, marginBottom: 32 }}>
          <h3 style={{ color: '#f87171', marginBottom: 12, fontSize: 14 }}>🔥 HOT LEADS</h3>
          {hotLeads.map(l => <div key={l.id} style={{ color: '#e0e0e0', fontSize: 13, marginBottom: 6 }}>{l.company} — {l.contact_email}</div>)}
        </div>
      )}
      <div style={{ background: '#1a1a1a', borderRadius: 8, padding: 20 }}>
        <h3 style={{ marginBottom: 16, fontSize: 14, color: '#888' }}>90-DAY SEND HISTORY</h3>
        <ResponsiveContainer width="100%" height={200}>
          <LineChart data={[...chart90].reverse()}>
            <XAxis dataKey="date" tick={{ fontSize: 10, fill: '#555' }} />
            <YAxis tick={{ fontSize: 10, fill: '#555' }} />
            <Tooltip contentStyle={{ background: '#1a1a1a', border: '1px solid #333' }} />
            <Line type="monotone" dataKey="emails_sent" stroke="#4ade80" dot={false} />
            <Line type="monotone" dataKey="replies" stroke="#60a5fa" dot={false} />
          </LineChart>
        </ResponsiveContainer>
      </div>
    </div>
  );
}
```

Create the remaining 7 pages as minimal functional shells that fetch data and render a table. Each follows the same pattern:

**dashboard/src/pages/LeadPipeline.jsx** — GET /api/leads, filterable table with status badges, click row to see detail.

**dashboard/src/pages/SendLog.jsx** — GET /api/send-log, paginated table of sent emails.

**dashboard/src/pages/ReplyFeed.jsx** — GET /api/replies, hot/schedule replies pinned first with color coding.

**dashboard/src/pages/SequenceTracker.jsx** — GET /api/sequences, per-lead step + next send date.

**dashboard/src/pages/CronStatus.jsx** — GET /api/cron-status, 9 job cards with last run time + status badge.

**dashboard/src/pages/HealthMonitor.jsx** — GET /api/health, bounce/unsub rate gauges.

**dashboard/src/pages/CostTracker.jsx** — GET /api/costs, daily stacked bar chart + table.

**dashboard/src/pages/ErrorLog.jsx** — GET /api/errors, unresolved badge, PATCH resolve action.

For each page, the minimal implementation is:
```jsx
import React, { useEffect, useState } from 'react';
import { api } from '../api';

export default function PageName() {
  const [data, setData] = useState(null);
  useEffect(() => { api.pageEndpoint().then(setData); }, []);
  if (!data) return <div>Loading...</div>;
  return (
    <div>
      <h1 style={{ marginBottom: 24, fontSize: 20 }}>Page Title</h1>
      <pre style={{ fontSize: 11, color: '#666' }}>{JSON.stringify(data, null, 2)}</pre>
    </div>
  );
}
```

Then progressively enhance with tables, charts, and actions.

- [ ] **Step 2: Commit**

```bash
git add dashboard/src/pages/
git commit -m "feat: add all 9 dashboard pages"
```

---

### Task 8.5: Build and verify

- [ ] **Step 1: Build React app**

Run: `cd dashboard && npm run build && cd ..`
Expected: `dashboard/dist/` created with `index.html` and assets.

- [ ] **Step 2: Run all backend tests one final time**

Run: `npx vitest run`
Expected: All tests PASS.

- [ ] **Step 3: Final commit**

```bash
git add -A
git commit -m "feat: Radar v2 complete — all 5 phases implemented"
```

---

## Execution Handoff

Plan complete. Ready to execute with superpowers:subagent-driven-development.

Each chunk is independent and can be executed by a fresh subagent:
- **Chunk 1** (Foundation) — no dependencies
- **Chunk 2** (AI Clients) — requires Chunk 1
- **Chunk 3** (findLeads) — requires Chunks 1+2
- **Chunk 4** (Anti-spam) — requires Chunk 1
- **Chunk 5** (Email Infra) — requires Chunk 1
- **Chunk 6** (Engines 3-5) — requires Chunks 1+2+4+5
- **Chunk 7** (Dashboard API) — requires Chunk 1
- **Chunk 8** (Dashboard React) — requires Chunk 7 (API must be running for proxy)

Recommended execution order: 1 → 2 → 4 → 5 → 3 → 6 → 7 → 8
